# Ev课程表 · 授权/激活服务端 QA 报告（第 10 轮）

- **仓库**：https://github.com/guomengtao/app-auth （main）
- **本轮 HEAD**：`e17ae99`
- **上轮 HEAD**：`e17ae99`（第 9 轮 QA 自己的文档提交）
- **代码变更**：**无**。`git fetch` 后 `FETCH_HEAD == HEAD`，`git status` 干净 → 按约定输出**短版「无变更确认报告 + 线上实测推进」**。
- **在线版本**：`/api/version` → `{"version":"1.5.51","patch":51}`（与本地 HEAD 一致，部署未滞后）

---

## 一、本轮结论摘要（一句话）

> **无代码变更、无新增 P0/P0-S、历史已修项零回归**；本轮唯一新增缺陷是 **P1-17：激活失败时服务端同步等邮件通知发完才返回，实测响应 2.3–9.4s、4 次请求中 2 次完全无响应（>60s / >20s），而前端 XHR 又没有 timeout 兜底 → 按钮可能长时间甚至永久卡在「激活中...」**；另 **P0-22 推进为「当前无买家受害」**（3 次线上拉单 `new/processed/skipped` 全为 0）。

**关键风险排序**：P0-22（爱发电 `AFDIAN_PLAN_MAP` 仍未证实，首个新买家是暴露点）> P1-17（本轮新增，全端型）> P1-16（埋点抢限流配额）。

---

## 二、业务链路走查结果表（10 条）

| # | 链路 | 结论 | 代码定位 | 推断的实际结果 |
|---|------|------|----------|----------------|
| 1 | 用户拿到激活码 → `activate.html` → 提交 → 激活成功 | ✅ **通** | `activate.html:386-408`（读 `#redeemCode`，字段 `redeemCode`）→ `api/activate.js:160,181,206` | 前端字段名与后端一致（`body.redeemCode`），非空、4 位格式、存在性三级校验齐全。成功分支无有效码无法线上实测，**代码路径与上轮一致** |
| 2 | 激活成功后读源与写源是否一致 | ✅ **通** | `api/activate.js:255` 读 `auth:redeem:<code>`、`:339` 查 `auth:activation:<code>`、写回同源 Redis | 本仓读写同一 Upstash Redis，不存在 split-brain。手环端激活状态属 `class-schedule` 仓库、100% 本地存储，**本仓侧不适用** |
| 3 | 同一激活码换设备 / 超出设备数 | ✅ **通** | `api/activate.js:230` `checkDeviceRateLimit`；`lib/rate-limit.js` `DEVICE_MAX_HITS = 50` | 阈值 50 次/天（P0-18 已复核），正常用户换机不会触发 |
| 4 | 已激活设备重复激活（幂等） | ✅ **通** | `api/activate.js:330-350`（`activationCodeReuse` + 查 `auth:activation:` 已存在则复用） | 幂等复用，不会重复扣次数、不报错 |
| 5 | 激活码无效 / 过期 / 被禁用 → 提示是否准确 | ✅ **通（文案）** / ⚠️ **慢（P1-17）** | `api/activate.js:225,274`；`lib/validate.js:3-12` | **本轮线上实测 3 种错误文案全部准确可见**：空值→「请输入兑换码」、格式错→「兑换码必须是 4 位大写字母或数字（A-Z, 0-9）」、不存在→「兑换码不存在或尚未同步到服务器，请在管理后台同步后重试」。**但返回耗时 2.3s / 9.4s / 2.3s，另有 2 次完全无响应** |
| 6 | 爱发电支付 → Webhook/拉单 → 自动发码 → 买家收到 | 🟡 **部分确认（当前无受害者）** | `api/afdian/query-orders.js:299-404`；`lib/afdian-processor.js:19,76-87` | 凭据已实证配置（上轮）。**本轮 3 次线上拉单均返回 `new_orders:0, processed:0, skipped:0`** → 没有任何订单卡在「未发码」状态（要么店铺暂无订单，要么历史订单全部已成功发码）。**`AFDIAN_PLAN_MAP` 仍无法匿名探测，风险转为「潜伏」** |
| 7 | 后台：登录 → 激活记录可见 → 生成兑换码 → 兑换码可用 → 删除/批量生效 | ⚪ **无法静态确认** | `api/admin/{records,redeem-codes}.js`；`admin_Dx23.html` | 需管理员凭据，无凭据不做爆破。上轮 UI 专项已确认 11 tab ↔ 11 panel 无空页面 |
| 8 | 后台：切换数据源（switch-db）后读写一致 | ⚪ **无法静态确认**（代码侧防线在位） | `lib/db-switches.js:205,229,236` 三处 `throw` | 写入失败会 throw 而非静默成功（P0-17 已修，本轮复扫在位）。实际一致性需后台操作验证 |
| 9 | 后台：直开（direct-activate）是否成功 | ⚪ **无法静态确认** | `api/admin/direct-activate.js` | 同上，需凭据 |
| 10 | 后台各栏目逐一点名（点了没反应 / 空白 / 报错） | ⚪ **无法静态确认** | `admin_Dx23.html`（11 tab） | 上轮 UI 专项实测无空白面板、157 个 onclick 全部有定义；本轮无代码变更，结论沿用 |

**汇总**：✅ 通 5 条 · 🟡 部分确认 1 条 · ⚪ 无法静态确认 4 条 · ❌ **不通 0 条**。
（4 条「无法静态确认」全部集中在需管理员凭据的后台功能，非缺陷，需作者/人工回归。）

---

## 三、缺陷清单

### 3.1 新增

| 编号 | 优先级 | 模块 | 标题 | 复现步骤（用户视角） | 期望结果 | 实际结果（推断/实测） | 影响端型 | 严重度 | 定位 | 修复建议 |
|------|--------|------|------|----------------------|----------|----------------------|----------|--------|------|----------|
| **P1-17** | P1 | 激活接口 + 激活页 | 激活失败时服务端同步等邮件发完才返回响应；前端 XHR 无 timeout 兜底 | 打开激活页，输入一个不存在的 4 位兑换码，点「激活」 | 1 秒内给出「兑换码不存在」提示，按钮恢复可点 | **实测**：响应耗时 **2.3s / 9.4s / 2.3s**；同日 7 次请求中 **2 次完全无响应**（1 次 >60s、1 次 >20s）。服务端挂起时 `xhr.onload` 永不触发 → 按钮永久停在「激活中...」，用户只能刷新页面重来 | ①②③④⑤ 全部（手环端弱网下概率最高） | 高（体验） | `api/activate.js:209`（`await notify.sendActivationFailure(...)` 在 `res.status(400)` 之前）；`lib/notify.js:206,272`（SMTP 15s / Resend 10s 超时）；`activate.html:216-259`（`xhrFetch` 无 `xhr.timeout` / `xhr.ontimeout`） | ① 把 `:209` 的 `await` 去掉，改成与 `pushNotification` 同款 fire-and-forget（末尾已有 `.catch(function(){})`，去掉 `await` 即可）；② `activate.html` 的 `xhrFetch` 加 `xhr.timeout = 15000;` + `xhr.ontimeout` 走 errorCallbacks 并 `setLoading(false)` |

> **为什么是 P1 不是 P0**：功能最终可用（响应到达后会正常提示），属「慢 + 偶发挂起」；但与 5 次/60s 的限流叠加时，用户连点会更快撞到 429，实际表现为「激活不上了」。

### 3.2 状态推进

| 编号 | 优先级 | 标题 | 上轮 | 本轮 | 证据 |
|------|--------|------|------|------|------|
| **P0-22** | P0 | 爱发电付款后买家收不到码 | ⏸ 半关闭（凭据已配，剩 `AFDIAN_PLAN_MAP`） | 🟡 **推进为「当前无买家受害」** | 本轮 3 次线上 `GET /api/afdian/query-orders` → `new_orders:0, processed:0, skipped:0`（耗时 6.5s / 3.6s / 1.3s）。`processBatchOrders` 只在订单 `processed=true` 时才 `continue` 不计数（`query-orders.js:32-34`），未映射/未付款的订单会每次重跑并计入 `skipped` → **skipped=0 证明当前不存在卡单**。`plan_map` 仍只能由后台 `?action=list`（需鉴权）看到 |
| **P1-16** | P1 | 埋点与激活共抢 IP 限流配额 | 🔴 未修（线上复现） | 🔴 **未修，本轮再次复现** | 纯 API 连续调用第 4 次即 `429 {"error":"请求过于频繁，请稍后再试"}`（5 次/60s，`lib/rate-limit.js:43-44`） |

---

## 四、端型兼容性对比表

无代码变更，本轮为快扫复核（箭头函数 / 可选链 / async-await / fetch 四类 ES6 语法计数）：

| 页面 | 小米手环 9 Pro | 小米手环 10 Pro | 移动端浏览器 | 微信内置浏览器 | 桌面浏览器 |
|------|----------------|----------------|--------------|----------------|------------|
| `activate.html` | ✅（ES6 全 0） | ✅ | ✅ | ✅ | ✅ |
| `index.html` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `login_aXs12.html` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `redeem-counts.html` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `ev-schedule.html` / `ev-timetable.html` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `user-guide.html` / `course-guide.html` | ✅ | ✅ | ✅ | ✅ | ✅ |
| `admin_Dx23.html` | ➖ 不适用 | ➖ 不适用 | ➖ 桌面定位 | ➖ 不适用 | ✅（允许 ES6） |

- ✅ = 无 JS 执行中断风险；本轮 8 个用户侧页面 **箭头函数 0、可选链 0、async/await 0、fetch 0**，P0-12 / P1-5~7 / P1-14 全部保持已修。
- ⚠️ 全部端型共有的新风险：**无 XHR 超时兜底（P1-17）**，手环端网络最差，受影响的概率最高。

---

## 五、数据源一致性

| 数据源 | 角色 | 本轮状态 |
|--------|------|----------|
| Upstash Redis | **权威源**，激活/兑换码/订单全部读写此处 | ✅ 读写同源，无 split-brain |
| Neon Postgres / Supabase | 由 05:00 cron 同步的副本 | ⚪ 无法匿名验证；本轮无代码变更，沿用上轮结论 |
| `data/*.json` | 死数据 | ✅ 全仓 `api/`、`lib/` 对 `data/*.json` 引用仍为 **0**（P3，无害） |
| Vercel Edge Config | 主库协调器标记 `auth_db_primary` | ✅ `lib/db-switches.js:3` key 为 `auth_db_primary`，写失败会 throw（`:205/229/236`） |

**结论**：数据源不一致**只在会导致用户激活状态错乱时才算 P0**。本轮无任何证据表明存在不一致，且主库切换失败已能正确抛错。

---

## 六、与上一轮对比

| 类别 | 内容 |
|------|------|
| **新增** | P1-17（失败响应被同步邮件通知阻塞 + XHR 无 timeout） |
| **已修复** | 无（本轮无代码变更） |
| **仍未修复** | P1-16（埋点抢限流配额）；P2-17 / P2-18 / P2-21 / P2-22（详见历史报告） |
| **状态推进** | P0-22 → 当前无买家受害，仅剩 `AFDIAN_PLAN_MAP` 待作者确认 |
| **历史已修项回归快扫** | ✅ **0 回归**：`?cron=1` 绕过 0 命中（`api/`、`lib/` 全扫）；8 个用户页 `fetch(` 全 0；`db-switches.js` 三处 `throw` 在位；`activate.html:222` `result.catch` 在位；`admin_Dx23.html` 解绑入口 12 处在位 |

---

## 七、回归测试建议（给开发的可执行 checklist）

按用户操作路径组织，本轮无代码变更，**只需跑加粗的 3 条**（其余上轮已验）：

1. **激活失败反馈速度**：`activate.html` 输入 `ZZZZ`（不存在）→ 点激活 → 秒表计时。**要求 < 1.5s 出提示且按钮恢复可点**；当前实测 2.3–9.4s。
2. **弱网/挂起兜底**：DevTools Network 限速到 Slow 3G，或直接把 `xhr` 请求 block 住 → 确认按钮 **不会永久停在「激活中...」**（应超时提示可重试）。
3. **爱发电 plan_map 确认**（最高业务优先）：后台「爱发电订单」tab → 看 `plan_map` 是否非空且覆盖在售 `plan_id`；为空则补配 `AFDIAN_PLAN_MAP`。
4. 连点保护：连续 5 次提交 → 确认文案是「请求过于频繁」而不是按钮卡死。
5. 手环真机：小米手环 9 Pro / 10 Pro 内置浏览器打开 `activate.html`，确认无白屏、按钮可点、键盘可输入。
6. 后台：登录 → 激活记录 → 生成兑换码 → 用该码在手环端激活 → 后台删除该码 → 再激活应失败。

---

## 八、已知风险·不急修（P4，仅记录）

| 编号 | 是什么 | 损失上限 | 触发条件 |
|------|--------|----------|----------|
| P4-A | 激活码为 4 位（A-Z/0-9），空间约 168 万，理论可爆破 | 少量未授权激活 | 需绕过 5 次/60s 的 IP 限流 |
| P4-B | `api/admin/me.js` 的 `logout` / `test-email` 分支在鉴权之前 | 可被匿名触发 SMTP 发信 | 需知道该后台路径 |
| P4-C | `api/afdian/query-orders` 同步分支免鉴权（P2-22） | 可被匿名消耗爱发电 API 配额、污染 cron 运行历史 | 直接 GET 该端点 |

> 按用户定位：知道就行，算一种福利，修复不着急。**不进交接本待办表，不排期。**

---

## 九、文档 vs 代码实测

| 文档说法 | 代码实际 | 结论 |
|----------|----------|------|
| `docs/edge-config-vs-redis-coordinator-analysis.md:155` 写环境变量 `EDGE_CONFIG_TOKEN` | `lib/db-switches.js` 读 `VERCEL_OIDC_TOKEN / VERCEL_TOKEN / VERCEL_TOKEN_ALT` | ⚠️ 文档与代码不符（沿用第 6 轮结论，本轮未改） |
| 同文档写 key `auth:db:primary` | 代码 `lib/db-switches.js:3` 为 `auth_db_primary` | ⚠️ 同上 |
| `admin_Dx23.html:4100` 环境变量说明表列了 `AFDIAN_PLAN_MAP` 及示例 | 代码 `lib/afdian-processor.js:19` 一致 | ✅ 一致 |

---

_本报告由 QA 生成，只提交文档、不修改源码。_
