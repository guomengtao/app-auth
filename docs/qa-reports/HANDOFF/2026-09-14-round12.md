# QA 交接本 · 第 12 轮（2026-09-14）

> 本轮 HEAD：`d13592d`（上轮 `9b32f07`，+38 提交 / 37 文件 / +17,168 行）
> 完整报告：[`../../../reports/app-auth-QA-20260914-1535.md`](../../../reports/app-auth-QA-20260914-1535.md)（仓库内副本：`../app-auth-QA-20260914-1535.md`）
> 线上版本：`1.5.51`

---

## 一句话

本轮 38 个提交几乎全在 EvNotifier 桌面客户端上；服务端的实质改动是「给所有通知调用加 `await`」，**方向对但把外部延迟直接转嫁给了用户** —— 上轮两个 P0 一个没修、一个更慢了。IP 限流**永久封禁**的根因本轮已精确定位到 `lib/redis.js:243`。

---

## 本轮待办（只放 P0 / P0-S / P1）

| # | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议修复顺序 |
|---|------|------|--------|------|------|-------------|
| 1 | **P0-23** | **IP 限流触发后永不解除** —— 用户被永久封禁，全系统无重置入口。本轮空闲 40 小时后 4 次请求**全部 429**，`Retry-After` 恒为 1 | **P0** | 🔴 未修（第 2 轮挂账） | `lib/redis.js:243`（`incr` SELECT 无过期过滤）、`:251-252`（ON CONFLICT 只更新 value）、`:270-277`（pttl 返回 -2）；`lib/rate-limit.js:48-60` | **1（最高）** |
| 2 | **P0-24** | **激活响应被 21 处同步外部调用阻塞**（本轮新增 11 处 `await pushNotification`）+ 前端 XHR 无 timeout → 按钮永久卡「激活中...」。实测 0.8s–25.2s | **P0** | 🔴 未修且**本轮恶化** | `api/activate.js` 9×`sendActivationFailure`(:167/195/217/241/266/288/312/414/534)、1×`sendActivationNotification`(:499)、**11×`pushNotification`(:175/203/225/249/274/296/320/400/422/513/543)**；`activate.html:224-258` | **2** |
| 3 | **P1-16** | 页面访问埋点与激活共抢同一 IP 限流配额（5 次/60s），用户实际只有 4 次试错机会 | P1 | 🔴 未修（第 4 次确认） | `activate.html:455` → `api/activate.js:28-29` 与 `:163`；`lib/rate-limit.js:43-44` | **3（改独立 key，1 行）** |
| 4 | **P1-18** | 后台统计用 `Promise.allSettled`（ES2020）无降级 → iOS<14 / 老微信内核上四张统计卡片全 0 | P1 | 🔴 新增·未修 | `admin_Dx23.html:4544`（`1d3f1f2` 引入） | **4（加 3 行 polyfill）** |

> P2/P3（P2-25 爱发电同步推送、P2-26 埋点同步化、P3-24 调试脚本入库、P3-25 逻辑重复实现）留在报告正文，不进本表。
> P4 白嫖类不进本表，见报告 §八「已知风险·不急修」。

---

## 需要你确认 / 提供的信息

1. **当前生产主库到底是 Upstash 还是 Postgres（Neon）？**
   第 8 轮（09-12 03:31）实测限流「60 秒自动恢复」，第 11 轮（09-12 22:40）起变成「永久封禁」，而这两轮之间**只有文档提交**。最可能的解释是主库从 Upstash 切到了 Postgres。如果是 Postgres，P0-23 就是 100% 必现；如果仍是 Upstash，那本轮实测需要另找解释。

2. **能否在后台加一个「清空 IP 限流」按钮？**
   目前 `grep "ratelimit:ip"` 全仓只命中定义处，**没有任何入口能重置**。用户被封后你本人也只能手工改数据库。

---

## 本轮已确认修复

| 编号 | 标题 | 复核方式 |
|------|------|---------|
| UI-2 | 后台 CDN（unpkg/jsdelivr）阻塞页面初始化 | ✅ 真修：`1d3f1f2` 把两个 `<script>` 从 `<head>` 移到 `</body>` 前并加 `preconnect`。代码复核通过 |

---

## 作者回复区

> Tom，请在这里填写本轮处理结论（我会按「打开代码复核」的方式逐条验证，不会只看文字）。

| 编号 | 是否已修 | 修复版本 / 提交 | 改动文件 | 说明 |
|------|---------|----------------|---------|------|
| P0-23 | | | | |
| P0-24 | | | | |
| P1-16 | | | | |
| P1-18 | | | | |

**其他说明：**

（此处可写你的判断、反驳、或需要我补充验证的点）

---

*第 12 轮 · 2026-09-14 15:35 · QA*
