# QA ↔ 作者 交接本 · 2026-09-10 Round 2

> **协作规则（为了让 git 永远不冲突）**
> - QA（WorkBuddy 自动化）：每轮**新建一个文件**，不改动历史文件。
> - 作者（Tom）：**只在本文件的「作者回复区」填写**，不要改上面的表格。
> - 双方写的是不同的文件 / 不同的区块 → 天然零冲突，历史完整留档。

- 本轮 HEAD：`98f3bb2`
- 完整报告：[`../app-auth-QA-20260910-1925.md`](../app-auth-QA-20260910-1925.md)

---

## 一、QA 本轮提出的待办

| # | 编号 | 标题 | 优先级 | 状态 | 定位 |
|---|------|------|--------|------|------|
| 1 | P0-9 | 单条删除兑换码 404（前端调 `DELETE /redeem-codes/{code}`，后端无 `[code].js` 路由） | P0 | ✅ 作者已修 `f81485f` | `admin_Dx23.html:5673` |
| 2 | P0-10 | 主库开关 split-brain：primary 禁用时写入落 B 库，05:00 同步按 A→B 覆盖，新付费记录被旧数据冲掉 | P0 | ⬜ 待处理 | `lib/postgres.js:17,23-29` |
| 3 | P0-6 | 破坏性脚本零护栏（`clear-db.js` / `force-sync.js` 无环境判定、无二次确认） | P0 | ⬜ 待处理 | `scripts/` |
| 4 | P0-8 | 通知器去重键不含订单号，同用户多订单会漏消息 | P0 | ⬜ 待处理 | `tools/ev-notifier/ev_notifier.py:224` |
| 5 | P0-11 | `activate.html` 用了 `async/await`，手环 9 Pro / 10 Pro 内置浏览器整段脚本不执行 → 白屏 | P0 | ⬜ 待处理 | `activate.html` |
| 6 | ~~P4-1~~ → **P0-S** | `?cron=1` 未鉴权即可触发跨库同步（含删键）→ 会导致激活数据被删、用户显示未激活，属「影响用户使用」，按新规则提级 | P0-S | ⬜ 待处理 | `api/admin/health.js` |

**QA 建议修复顺序**：P0-10 → **P0-S（原 P4-1）** → P0-6（你已修） → P0-8 → P0-11

---

## 优先级口径说明（2026-09-10 更新，后续各轮均按此执行）

项目刚运营、用户体量几十人、精力有限，因此：

1. **第一优先 = 用户能不能真的用起来**。激活能不能成功、后台按钮点了是不是真生效、编辑/删除是不是真落库、已有功能是不是确实能用。这些排最前。
2. **漏洞分两类**：
   - **影响用户使用的（不能用 / 数据丢 / 页面挂）→ 必须修**，直接提到 P0-S，不放 P4。本文件第 6 条就是这个情况。
   - **只造成资金损失的（白嫖 / 免单 / 伪造激活码 / 爆破）→ 只记录、不催修**，单独放报告末尾「已知风险·不急修」，不进本待办表，不排期、不给修复方案。
3. **不再展开**：并发、限流、性能优化、密钥轮换、合规审计、隐蔽路径加固。对这些最多一句话带过。
4. 手环 9 Pro / 10 Pro 内置浏览器问题按 P1 顶格处理（语法不兼容会整段脚本不执行 → 白屏）。

---

## 二、作者回复区

> 在这里逐条写结论。QA 下一轮会自动读取本文件，把已处理的条目标记为已修复并复核代码。

### P0-10 主库开关 split-brain
- 结论：（修 / 不修 / 延后）
- 说明：

### P0-6 破坏性脚本零护栏
- 结论：
- 说明：

### P0-8 通知器去重键
- 结论：
- 说明：

### P0-11 手环浏览器兼容
- 结论：
- 说明：

### P4-1 / P0-S cron 鉴权绕过
- 结论：
- 说明：

### 其他想对 QA 说的
-
