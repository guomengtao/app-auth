# QA ↔ 作者 交接本 · 2026-09-11 Round 3

> **协作规则（为了让 git 永远不冲突）**
> - QA（WorkBuddy 自动化）：每轮**新建一个文件**，不改动历史文件。
> - 作者（Tom）：**只在本文件的「作者回复区」填写**，不要改上面的表格。
> - 双方写的是不同的文件 / 区块 → 天然零冲突，历史完整留档。

- 本轮 HEAD：`62d724a`（上轮基线 `98f3bb2`，+13 提交）
- 完整报告：[`../app-auth-QA-20260911-0154.md`](../app-auth-QA-20260911-0154.md)

---

## 一、上轮（Round 2）条目复核结果

| # | 上轮编号 | 标题 | 状态 | 复核方式（打开代码确认，非仅看文档） |
|---|---------|------|------|------|
| 1 | P0-9 | 单条删除兑换码 404 | ✅ **已修复** | `admin_Dx23.html:5673` 改 `?code=` query；`api/admin/redeem-codes.js:487` DELETE 分支正确读取 |
| 2 | P0-10 | 主库开关 split-brain | ✅ **已修复** | `api/admin/health.js:1284-1289` 明确拒绝禁用当前主库（"Cannot disable the primary database"），触发路径封死 |
| 3 | P0-6 | 破坏性脚本零护栏 | ✅ **已修复** | `scripts/clear-db.js:7-17`、`force-sync.js:32-40` 三重护栏：NODE_ENV=production 拒绝 + 必须 `--confirm` + 交互输入 `YES_DELETE_ALL` |
| 4 | P0-8 | 通知器去重键不含订单号 | ⬜ **仍未修** | `tools/ev-notifier/ev_notifier.py:225` 仍为 `mid = f"{ts}_{mtype}"`。降为 P2（概率低，仅影响通知器本地显示，不影响发码） |
| 5 | P0-11 | `activate.html` 手环兼容 | ⚠️ **只修一半** | `c758469` 去掉了 `async/await`，但 `activate.html:312` 仍在用 `fetch` → 手环端点激活仍无反应。**改编号 P0-12 重新提出** |
| 6 | P0-S-1（原 P4-1） | `?cron=1` 鉴权绕过 | ⬜ **仍未修** | `api/admin/health.js:573` 逻辑未变：`if (!auth.authorized && !isCron)`，带 `cron=1` 即完全免鉴权 |

---

## 二、本轮待办（只放 P0 / P0-S / P1）

| # | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议 |
|---|------|------|--------|------|------|------|
| 1 | **P0-12** | 手环端点「激活」按钮无反应（`activate.html` 仍用 `fetch`，老内核无此 API） | P0 | ⬜ 待处理 | `activate.html:312` | 改 `XMLHttpRequest` 封装或加 fetch polyfill |
| 2 | **P0-13** | 兑换码换设备后永久锁死，全系统无解绑能力 | P0 | ⬜ 待处理 | `api/activate.js:352-359` | 后台加「解绑/重置设备」按钮；最低成本先改错误文案告知用户联系你 |
| 3 | **P0-S-1** | `?cron=1` 完全绕过鉴权，可远程触发跨库同步（含删键） | P0-S | ⬜ 待处理 | `api/admin/health.js:573` | 改校验 Vercel Cron 的 `Authorization: Bearer $CRON_SECRET` 请求头 |
| 4 | P1-4 | `index.html:140` 语法错误（缺右括号+缺逗号），访客统计 100% 失效 | P1 | ⬜ 待处理 | `index.html:140` | 补 `)` 与参数间逗号 |
| 5 | P1-5 | `login_aXs12.html` 仍用 `fetch` → 手环端登录态检查失败 | P1 | ⬜ 待处理 | `login_aXs12.html:122` | 同 P0-12，改 XHR |
| 6 | P1-6 | `redeem-counts.html` 仍用 `fetch` → 手环端计数不显示 | P1 | ⬜ 待处理 | `redeem-counts.html:106` | 同 P0-12，改 XHR |
| 7 | P1-7 | `ev-schedule.html` 用 `const` + `IntersectionObserver` → 手环端脚本可能整段挂 | P1 | ⬜ 待处理 | `ev-schedule.html:1093,1111,1119` | `const`→`var`；IO 加存在性判断 |
| 8 | P1-8 | `admin_Dx23.html` 163 处 ES6 → 手环端不可用 | P1 | ⬜ 待处理（低） | `admin_Dx23.html` | 后台本为桌面设计，**如无手机处理售后需求可不做** |

**QA 建议修复顺序**：**P0-12 → P0-13 → P0-S-1** → P1-4（5 分钟可修）→ P1-5/6/7（与 P0-12 同批改 XHR 即可）→ P1-8（可不做）

> 说明：P0-12 / P1-5 / P1-6 是同一个根因（未做 fetch→XHR 兼容），**建议一次批处理**，改完手环端三个页面同时恢复。

---

## 三、本轮确认「不是缺陷」（避免你重复排查）

- 函数合并重构（13→10 个函数）后，前端对 `switch-db` / `direct-activate` / `sse` 的调用**已全部同步到新路径**，未留下悬空调用。
- 前端 11 个后台 tab 与 panel 一一对应、初始化函数齐全；`section=` 参数与后端实现**逐一核对全部匹配**（含 `/api/admin/stats` 的 trends / visitor-* 四项）。后台无空白面板。
- 爱发电支付 → 验签 → 发码 → 私信链路**完整且有幂等保护**（`afdian:processed`）。
- 已激活设备重复激活**幂等正常**（返回同一激活码并按剩余有效期续期）。

---

## 四、作者回复区

> 在这里逐条写结论。QA 下一轮会自动读取本文件，并**打开代码复核**是否真修（不会只看你写"已修"）。

### P0-12 手环端 fetch 兼容
- 结论：（修 / 不修 / 延后）
- 说明：

### P0-13 兑换码换设备锁死 / 解绑
- 结论：
- 说明：

### P0-S-1 `?cron=1` 鉴权绕过
- 结论：
- 说明：

### P1-4 index.html 语法错误
- 结论：
- 说明：

### P1-5 / P1-6 / P1-7 手环端兼容
- 结论：
- 说明：

### P1-8 后台 ES6 改造
- 结论：
- 说明：

### 其他想对 QA 说的
-
