# QA 交接 · 第 10 轮（2026-09-12 16:17）

- **本轮 HEAD**：`e17ae99`
- **上轮 HEAD**：`e17ae99`（第 9 轮 QA 文档提交）
- **代码变更**：**无**。`git fetch` 后 `FETCH_HEAD == HEAD`，`git status` 干净 → 本轮为「无变更确认 + 线上实测推进」短版。
- **完整报告**：[`../../app-auth-QA-20260912-1617.md`](../../app-auth-QA-20260912-1617.md)

---

## 一、本轮结论（一句话）

> **无代码变更、无新增 P0/P0-S、历史已修项零回归**；本轮价值在两点：**新增 P1-17（激活失败响应被同步邮件通知阻塞，实测 2.3–9.4s、两次完全无响应，且前端 XHR 无 timeout 兜底 → 按钮可能永久卡「激活中...」）**，以及 **P0-22 推进为「当前无买家受害」**（3 次线上拉单 `new/processed/skipped` 全 0）。

---

## 二、上轮待办复核

| 编号 | 标题 | 上轮状态 | 本轮复核结论 | 证据 |
|------|------|----------|--------------|------|
| **P0-22** | 爱发电付款后买家可能收不到兑换码 | 🔴 待作者确认 | 🟡 **推进：当前无受害者，仅剩 `AFDIAN_PLAN_MAP` 未证实** | 本轮 3 次线上 `GET /api/afdian/query-orders` → `{"new_orders":0,"processed":0,"skipped":0,"synced_at":...}`（6.5s / 3.6s / 1.3s）。`query-orders.js:32-34` 只有 `processed=true` 才 `continue` 不计数，未映射/未付款订单会每次重跑并计入 `skipped` → **skipped=0 证明当前没有订单卡在未发码状态**。风险转为「潜伏」，首个新买家是暴露点 |
| **P1-16** | 埋点与激活共抢 IP 限流配额 | 🔴 未修（已复现） | 🔴 **未修，本轮再次复现** | 纯 API 连续调用第 4 次 → `429 {"error":"请求过于频繁，请稍后再试"}`（5 次/60s，`lib/rate-limit.js:43-44`） |

---

## 三、本轮待办（只列 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|------|------|--------|------|------|----------|
| **P0-22** | 爱发电发码：`AFDIAN_PLAN_MAP` 是否配置未知；漏配则买家付款后订单被 `no_product_mapping` skipped、收不到码且无提示 | P0 | ⏸ 待确认（只剩这一项；当前无受害者） | `lib/afdian-processor.js:19`、`:76-87` | **1（后台「爱发电订单」tab 看 `plan_map` 是否覆盖在售 `plan_id`）** |
| **P1-17** | 激活失败时服务端同步等邮件通知发完才返回（实测 2.3–9.4s，2 次完全无响应）；前端 XHR 无 timeout 兜底 → 按钮可能永久卡「激活中...」 | P1 | 🔴 新增·未修 | `api/activate.js:209`（`await notify.sendActivationFailure`）；`lib/notify.js:206,272`；`activate.html:216-259`（无 `xhr.timeout`） | **2（去掉 `await` + 加 `xhr.timeout`，各 1 行）** |
| **P1-16** | 页面访问埋点与激活共抢同一 IP 限流配额（5 次/60s） | P1 | 🔴 未修（两次线上复现） | `activate.html:457` → `api/activate.js:29` 与 `:164`；`lib/rate-limit.js:43-44` | **3（改独立 key 即可，1 行）** |

> P2/P3 全部留在报告正文，不进本表；P4 白嫖类只在报告末尾「已知风险·不急修」，不进本表。

---

## 四、历史已修项回归快扫（本轮 0 回归）

| 项 | 结果 |
|------|------|
| P0-12 手环端 `fetch` 残留 | ✅ 8 个用户侧页面 `fetch(` 全 0；箭头函数 / 可选链 / async-await 也全 0 |
| P0-S-1 `?cron=1` 绕过 | ✅ `api/`、`lib/` 全扫 0 命中 |
| P0-17 switch-db 静默成功 | ✅ `lib/db-switches.js:205/229/236` 三处 `throw` 在位 |
| P0-21 非 JSON 卡按钮 | ✅ `activate.html:222` `result.catch` 在位 |
| P0-13 解绑入口 | ✅ `admin_Dx23.html` 12 处 `unbind/Unbind` 在位 |
| P4/P3 死数据 `data/*.json` | ✅ `api/`、`lib/` 引用仍为 0 |

---

## 五、作者回复区

> Tom，请直接在下面填写（或更简单：commit 里写 `fix(qa): P1-17 xxx`，我会自动扫描编号）。
> 「认为不是问题」和「无法静态确认」的两类请务必回一句，否则下轮会重复报。

### ① 已处理（附 commit）

| 编号 | commit | 说明 |
|------|--------|------|
|  |  |  |

### ② 处理中

| 编号 | 计划 | 预计版本 |
|------|------|----------|
|  |  |  |

### ③ 待处理 / 不打算做

| 编号 | 决定 | 理由 |
|------|------|------|
|  |  |  |

### ④ 对本轮结论的异议 / 补充

**本轮只需要你回答两个问题**：

1. 后台「爱发电订单」tab 里的 **`plan_map` 是不是空的**？是否为每个在售套餐都配了 `plan_id`？
   （`AFDIAN_USER_ID` / `AFDIAN_TOKEN` 我第 9 轮已线上确认配好，不用再答。本轮实测当前**没有任何订单卡在未发码状态**，所以即使没配，现在也还没人受害——但下一个买家就会踩到。）
2. **P1-17 认不认**？我这边实测 7 次请求里有 2 次服务端完全不返回（>60s / >20s），如果是 SMTP 发信慢导致的，把 `api/activate.js:209` 的 `await` 去掉就能立刻降到毫秒级。

---

_本文件由 QA 新建，只增不改。QA 不修改源码。_
