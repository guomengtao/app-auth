# QA 交接本 · 2026-09-11 Round 6

> - 作者（Tom）：**只在本文件的「作者回复区」填写**，不要改上面的表格。
> - 双方写的是不同的文件 / 区块 → 天然零冲突，历史完整留档。

- 本轮 HEAD：`e3c28c2`（上轮基线 `cad08ce`，+7 提交 / 13 文件）
- 完整报告：[`../app-auth-QA-20260911-2101.md`](../app-auth-QA-20260911-2101.md)

---

## 一、上轮（Round 5）条目复核结果

> 全部**打开代码复核**，不采信"已修"字面结论。Round 5 的「作者回复区」本轮仍为空白。

| # | 上轮编号 | 标题 | 状态 | 复核证据（代码定位） |
|---|---------|------|------|------|
| 1 | **P0-16** | 激活页点一次后按钮永久卡死 | ❌ **未修（线上仍在）** | `activate.html:213` 仍只挂 `result.then`，无 `catch`/`finally`；`curl https://app-auth.gudq.com/activate.html` 第 213 行确认线上同款。**注：本轮修正了上轮的描述** —— 实测（Node 复现）激活码**是能正常显示**的，坏的只是按钮不再复位，所以真实症状是「首次输错后无法重试，必须刷新页面」而不是「激活码永不显示」 |
| 2 | **P0-15** | CRON_SECRET 是否已配置 | ⚠️ **仍待确认** | `health.js:458-459`；全仓除代码外无任何配置文档/脚本提及 |
| 3 | **P1-11** | 三页 CSS 逗号被吞 | ❌ **未修** | `index.html:11/19/24/54/60/61`；`user-guide.html:11/20/23`；`course-guide.html:11/20/23` 全部原样 |
| 4 | **P1-12** | 手册只有一个下载链接 | ❌ **未修** | `user-guide.html:201` 仍只有 7778 |
| 5 | **P1-13** | 手册与代码冲突 / 无联系方式 | ❌ **未修** | `user-guide.html:287` 原文未改；全站 grep `QQ群\|微信群` 零命中 |
| 6 | **P1-14** | 两页残留 ES6 | ❌ **未修** | `login_aXs12.html:99/125`；`redeem-counts.html:87` 原样 |
| 7 | **P1-15** | 复制按钮无降级 | ❌ **未修** | `activate.html:333` 仍裸调 `navigator.clipboard`（对比 `admin_Dx23.html:8235` 已有守卫） |

**说明**：上轮 7 条待办**无一处理**，本轮 7 个提交全部集中在「去通知化 + 主库协调器换 Edge Config」。理解你在做架构收敛，但 P0-16 是**一行改动**，建议优先止血。

---

## 二、本轮待办（只放 P0 / P0-S / P1）

| # | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议 |
|---|------|------|--------|------|------|------|
| 1 | **P0-17**（新） | 后台「切换主库」永远返回成功，实际可能什么都没存 → 不同实例读写不同库的风险 | P0 | ⬜ 待处理 | `lib/db-switches.js:175-209`（`savePrimary`）；调用点 `api/admin/health.js:1654` | ① `savePrimary` 失败要 `throw`（恢复旧版行为）；② `!resp.ok` 时 `console.error` 并返回 false；③ `:1654` 检查返回值；④ 启动期自检：`EDGE_CONFIG` / 写入 token 缺失打 WARNING。**顺手核对 §三 的三个文档/代码不一致，它们很可能就是根因** |
| 2 | **P0-16** | 激活页点一次后按钮永久卡「激活中…」，无法二次提交 | P0 | ⬜ 待处理 | `activate.html:213`、`:404`、`:409`、`:297-306` | 最快一行：`result.catch = function(){ return result; };` 加在 `:213` 后面。更彻底：换回 `new Promise(...)`。另把 `setLoading(false)` 挪进 `xhr.onload`/`xhr.onerror` |
| 3 | **P0-15** | **待你确认**：CRON_SECRET 若未配置，每日 04:00 备份 / 05:00 同步永久 401，后台「立即执行」同样 401 | P0（待确认） | ⬜ 待确认 | `api/admin/health.js:458-459`、`:2213`；`vercel.json` crons | Vercel → Settings → Environment Variables 设 `CRON_SECRET`（`openssl rand -hex 32`）；设完对备份/同步各点一次「立即执行」确认 success |
| 4 | **P1-11** | 首页/用户手册/教程 三页 CSS 逗号被吞 | P1 | ⬜ 待处理 | `index.html:11/19/24/54/60/61`；`user-guide.html:11/20/23`；`course-guide.html:11/20/23` | 补回逗号：`linear-gradient(135deg, #0f0c29, #302b63, #24243e)`、`rgba(255,255,255,0.97)`、`rgba(0,0,0,0.5)`、`rgba(108,99,255,0.15)` |
| 5 | **P1-13** | 手册说换设备可用同码重新生成，代码实际拒绝；"联系作者"全站无联系方式 | P1 | ⬜ 待处理 | 文档 `user-guide.html:287`；代码 `api/activate.js:348`/`:357` | 改手册 + 把 QQ 群 936886288 写进手册和激活页错误提示 |
| 6 | **P1-12** | 手册只给一个下载链接（10 Pro 方屏） | P1 | ⬜ 待处理 | `user-guide.html:201` | 按机型列 5 个：7778 / 7828 / 7829 / 7830 / 7877 |
| 7 | **P1-14** | `login_aXs12.html` / `redeem-counts.html` 残留 Promise / URLSearchParams | P1 | ⬜ 待处理 | `login_aXs12.html:99/125`；`redeem-counts.html:87` | 复用 `activate.html:194-237` 的 `parseQueryParams` + XHR 封装（**记得带 `catch`**） |
| 8 | **P1-15** | 「复制激活码」用 `navigator.clipboard` 无降级 | P1 | ⬜ 待处理 | `activate.html:333` | 加 `document.execCommand('copy')` 降级分支 |
| 9 | **P0-18**（新） | 设备激活限流：成功与失败都计数，3 次/24h 太少，正常用户会被自己耗光额度 | P0 | ⬜ 待处理 | `lib/rate-limit.js:27-28`（`DEVICE_MAX_HITS=3`）、`:44-56`；调用点 `api/activate.js:214` | ① **成功激活后 `redis.del("ratelimit:device:"+key)` 清零**；② 幂等复用分支（`activate.js:280-331`）不计数；③ 阈值 3→10、窗口 24h→1h |
| 10 | **P0-19**（新） | 限流计数在 Postgres 模式下可能永不归零 → 设备被**永久**锁定，但提示仍是"请 24 小时后再试" | P0（待确认主库） | ⬜ 待确认 | `lib/redis.js:240` 的 `incr` 缺少 `AND (expires_at IS NULL OR expires_at > NOW())`（对比 `:138` 的 `get` 是有的）；`purgeExpired()` 只在 `get`/`mget` 里调，`incr` 不调 | ① `incr` 的 SELECT 补上过期过滤；② 或在 `incr` 开头也调一次 `purgeExpired()`。**先确认线上 `USE_UPSTASH`/`DB_PROVIDER`** —— 若用真 Upstash（原生 TTL）则无此问题 |
| 11 | **P0-20**（新） | 限流 key 用客户端传入的 deviceId，可被伪造：换 ID 就又有 3 次；也可拿真实用户 deviceId 刷满把他锁死 | P0-S | ⬜ 待处理 | `lib/rate-limit.js:46`；deviceId 明文出现在激活页 URL `?deviceId=xxx` | 增加兑换码维度联合限流；后台加「清除设备限流」应急解锁按钮 |

**QA 建议顺序**：
1. **P0-17 先花 5 分钟自查**（Edge Config 里有没有 `auth_db_primary` 这个 key？环境变量里有没有 `VERCEL_OIDC_TOKEN`/`VERCEL_TOKEN`？）—— 如果两个都没有，说明主库切换功能目前**完全不生效**，这是本轮最大风险。
2. **P0-16 一行止血**（线上正在发生）。
3. **P0-15 一分钟确认**。
4. **P0-18 顺手改**（成功清零 + 放宽阈值，改动小、收益大）。
5. P1 四条本周内顺手清掉。

> **关于 P0-18 / P0-19 / P0-20（设备激活限流）**：这三条是你问"该设备激活次数过多，请 24 小时后再试"时顺带挖出来的。详见报告 §十一。
> 最值得注意的是 **P0-19**：`lib/redis.js` 的 Postgres 版 `incr` 没过滤 `expires_at`，24 小时到期后计数不归零而是继续累加 → 用户**永久**被锁，但提示还是"24 小时后再试"。**请确认线上 `USE_UPSTASH` / `DB_PROVIDER` 用的是哪种模式** —— 真 Upstash 有原生 TTL 不受影响。

---

## 三、本轮确认「不是缺陷」（避免你重复排查）

- **去通知化改动是干净的**：`lib/push-notify.js`、`api/sse.mjs` 已删除，全仓**无悬挂引用**（grep 仅历史 QA 文档命中）；`api/afdian/query-orders.js` 删除 `pushNotify` 后 `node --check` 通过（只留了个空行）。
- 后台 11 个 tab ↔ 11 个 panel 一一对应，删除通知 UI **未产生空白面板**。
- `api/ lib/ scripts/ tools/` 下所有 `.js` / `.mjs` **全部通过 `node --check`（0 失败）**。
- 激活业务逻辑本体（幂等复用、换设备拒绝、时长合并、错误文案、trim + 转大写归一）**全部正确**。
- 「禁用兑换码」场景不存在 —— 全仓无 `disabled` 字段，兑换码只有"未使用/已使用"两态。
- `scripts/clear-db.js:12`、`scripts/force-sync.js:37` 的 `--confirm` 护栏仍在。
- 解绑能力仍在（`admin_Dx23.html:4810` 解绑 / `:5275` Unbind）。

---

## 四、作者回复区

> 在这里逐条写结论。QA 下一轮会自动读取本文件，并**打开代码复核**是否真修（不会只看你写"已修"）。

### P0-17 后台切换主库静默不生效（本轮新增）
- 结论：
- Edge Config 里 `auth_db_primary` 这个 key 存在吗？（存在 / 不存在 / 没看过）
- Vercel 环境变量里有 `VERCEL_OIDC_TOKEN` 或 `VERCEL_TOKEN` 吗？（有 / 没有 / 没看过）
- 说明：

### P0-16 激活页按钮卡死
- 结论：
- 说明：

### P0-15 CRON_SECRET 是否已配置
- 结论：（请明确写「已设置」或「未设置」）
- 说明：

### P1-11 三页 CSS 逗号被吞
- 结论：
- 说明：

### P1-13 换设备手册与代码冲突 / 缺联系方式
- 结论：
- 说明：

### P1-12 手册下载链接
- 结论：
- 说明：

### P1-14 login / redeem-counts 残留 ES6
- 结论：
- 说明：

### P1-15 复制按钮降级
- 结论：
- 说明：

### 其他想对 QA 说的
-
