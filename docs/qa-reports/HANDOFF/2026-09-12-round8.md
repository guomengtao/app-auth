# QA 交接 · 第 8 轮（2026-09-12 03:31）

- **本轮 HEAD**：`e15d857`（v1.5.51）
- **上轮 HEAD**：`258d7aa` → 本轮 +12 提交 / 14 文件 / +1159 -106
- **完整报告**：[`../../app-auth-QA-20260912-0331.md`](../../app-auth-QA-20260912-0331.md)（同仓库 `docs/qa-reports/`）
- **线上版本**：`https://app-auth.gudq.com/api/version` → `1.5.51`（已部署）

---

## 一、本轮结论（一句话）

> 上轮 4 条 P0/P0-S **全部真修并线上实证**，本轮**未发现新的 P0/P0-S**；唯一影响用户的是 **P1-16 埋点与激活共抢 IP 限流配额**，另有 **P0-22 需作者确认 3 个爱发电环境变量**。

业务链路走查 10 条：**8 条通 / 1 条不适用 / 1 条无法静态确认**，无「不通」。

---

## 二、上轮待办复核（作者提交 `19883d2` + `b75dcc7`）

| 编号 | 标题 | 上轮状态 | 本轮复核结论 | 证据 |
|------|------|----------|--------------|------|
| **P0-S-1** | `?cron=1` 全量鉴权绕过 | 🔴 未修（回归） | ✅ **已修** | 线上 `?section=stats&cron=1` → 401、`?section=records&cron=1` → 401；`isCron` 只认 Bearer，`isCronBackup` 收窄到 backup |
| **P0-21** | 非 JSON 响应按钮永久 loading | 🔴 未修 | ✅ **已修** | `activate.html:249` JSON.parse 已 try/catch；`:252` errorCallbacks 真实注册；onload/onerror 双兜底 setLoading(false) |
| **P0-15** | CRON_SECRET 未配 → 04:00/05:00 静默 401 | ⏸ 挂起（多轮） | ✅ **已解决，建议关闭** | 线上 `?section=backup&cron=1` → **200 且真跑了一次备份**；`health.js:487` 该分支要求 `cronSecret !== ""` → 反证 CRON_SECRET 已配。Vercel Cron 配了 CRON_SECRET 会自动带 Bearer，故 05:00 sync 亦通过 |
| **P0-17** | switch-db 永远返回成功 | 🟡 已修待验 | ✅ **确认已修** | `lib/db-switches.js:205/229/236` 三处 throw；`health.js:1687` 未捕获 → 500 带原因 |
| **P0-13 / P1-9** | 换设备锁死、无解绑入口 | 🟡 已修待验 | ✅ **确认已修** | 后台「兑换码」`:4818` +「激活记录」`:5283` 两个按钮；接口 `api/admin/redeem-codes.js:302-327` |
| **P0-14** | 后台「立即执行」401 | 🟢 已验证 | ✅ **未回归** | `DEFAULT_TASKS.vercelPath` 已无 `&cron=1`；run-task 带 `Authorization: Bearer $CRON_SECRET`（`:2243-2248`） |
| **P0-18** | 设备限流阈值过严 | 🔴 待处理 | ✅ **实质消除** | `DEVICE_MAX_HITS` 3 → 50（`lib/rate-limit.js:46`），key 带日期每天自动换（`:64`） |
| **UI-2** | CDN 拖垮后台初始化 | 🟢 已验证 | ✅ **未回归** | `admin_Dx23.html:4481`、`:7149` 均有 `typeof lucide !== 'undefined'` 守卫 |
| — | `DEVICE_WINDOW_MS` 悬空导出致全站 500 | （本轮 hotfix） | ✅ 作者自修 | `b75dcc7` 已移除导出 |

---

## 三、本轮待办（只列 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|------|------|--------|------|------|----------|
| **P0-22** | 爱发电付款后买家可能收不到兑换码（依赖 `AFDIAN_USER_ID` / `AFDIAN_TOKEN` / `AFDIAN_PLAN_MAP`） | P0 | 🔴 待作者确认 | `lib/afdian-processor.js:19`、`:170-190`；`lib/afdian-api.js:8-10` | **1（先确认环境变量）** |
| **P1-16** | 页面访问埋点与激活共抢同一 IP 限流配额（5 次/60s），用户输错 4 次即 429 | P1 | 🔴 未修 | `activate.html:457` + `api/activate.js:29`、`:164`；`lib/rate-limit.js:43-44` | **2** |

> P2/P3 全部留在报告正文，不进本表；P4 白嫖类只在报告末尾「已知风险·不急修」，不进本表。

---

## 四、作者回复区

> Tom，请直接在下面填写（或更简单：commit 里写 `fix(qa): P0-22 xxx` / `fix(qa): P1-16 xxx`，我会自动扫描编号）。
> 「认为不是问题」和「无法静态确认」的两类请务必回一句，否则下轮会重复报。

### ① 已处理（附 commit）

| 编号 | commit | 说明 |
|------|--------|------|
|  |  |  |

### ② 处理中

| 编号 | 计划 | 预计版本 |
|------|------|----------|
|  |  |  |

### ③ 待处理 / 不打算做

| 编号 | 决定 | 理由 |
|------|------|------|
|  |  |  |

### ④ 对本轮结论的异议 / 补充

（P0-22 请在这里直接告诉我三个环境变量是否已配：`AFDIAN_USER_ID` / `AFDIAN_TOKEN` / `AFDIAN_PLAN_MAP`）

---

_本文件由 QA 新建，只增不改。QA 不修改 `src/`、`api/`、`lib/` 源码。_
