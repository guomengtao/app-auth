# QA 交接 · 第 9 轮（2026-09-12 09:59）

- **本轮 HEAD**：`75c8e93`
- **上轮 HEAD**：`e15d857`（v1.5.51）
- **代码变更**：**无**。`e15d857..75c8e93` 只有 QA 第 8 轮报告 + 交接本，无源码改动。
- **完整报告**：[`../../app-auth-QA-20260912-0959.md`](../../app-auth-QA-20260912-0959.md)

---

## 一、本轮结论（一句话）

> **无代码变更、无新增 P0/P0-S、历史已修项零回归**；本轮价值在两点线上实测：**P0-22 的爱发电凭据已实证配置（只剩 `AFDIAN_PLAN_MAP` 待确认）**，**P1-16 线上复现「开 2 次页面后第 4 次激活就被 429」**。

---

## 二、上轮待办复核

| 编号 | 标题 | 上轮状态 | 本轮复核结论 | 证据 |
|------|------|----------|--------------|------|
| **P0-22** | 爱发电付款后买家可能收不到兑换码 | 🔴 待作者确认 | 🟡 **半关闭：凭据已确认配置** | 线上 `GET /api/afdian/query-orders` → `{"success":true,...,"synced_at":1789178166032}`，`synced_at` 只在 `api/afdian/query-orders.js:404`（位于 `isConfigured()` 闸门之后）→ 反证 `AFDIAN_USER_ID` + `AFDIAN_TOKEN` 均已配置。**仅剩 `AFDIAN_PLAN_MAP` 无法匿名探测** |
| **P1-16** | 埋点与激活共抢 IP 限流配额 | 🔴 未修 | 🔴 **未修，且本轮线上复现** | 实测：`visitor-track` ×2 → 激活第 1/2/3 次 400（文案正确）、**第 4 次即 429**。用户实际只有 3 次试错机会 |

---

## 三、本轮待办（只列 P0 / P0-S / P1）

| 编号 | 标题 | 优先级 | 状态 | 定位 | 建议顺序 |
|------|------|--------|------|------|----------|
| **P0-22** | 爱发电发码：`AFDIAN_PLAN_MAP` 是否配置未知，漏配则买家付款后订单被 `no_product_mapping` skipped、收不到码且无提示 | P0 | ⏸ 待确认（只剩这一项） | `lib/afdian-processor.js:19`、`:76-87` | **1（后台「爱发电订单」tab 看 plan_map 是否覆盖在售 plan_id）** |
| **P1-16** | 页面访问埋点与激活共抢同一 IP 限流配额（5 次/60s） | P1 | 🔴 未修（线上已复现） | `activate.html:457` → `api/activate.js:29` 与 `:164`；`lib/rate-limit.js:43-44` | **2（改独立 key 即可，1 行）** |

> P2/P3 全部留在报告正文，不进本表；P4 白嫖类只在报告末尾「已知风险·不急修」，不进本表。
> 本轮新增 P2-22（拉单端点免鉴权）为低优先级，不进本表。

---

## 四、历史已修项回归快扫

| 项 | 结果 |
|------|------|
| P0-12 手环端 `fetch` 残留 | ✅ 8 个用户侧页面 `fetch(` 全 0 |
| P0-S-1 `?cron=1` 绕过 | ✅ 线上 `?section=stats&cron=1` → 401 |
| P0-21 非 JSON 卡按钮 | ✅ 线上 `activate.html` 含 `errorCallbacks`×3 / `setLoading(false)`×3 |
| P0-17 switch-db 静默成功 | ✅ `lib/db-switches.js:205/229/236` 三处 `throw` 在位 |
| UI-2 CDN 拖垮后台 | ✅ `typeof lucide` 守卫 2 处在位 |
| `DEVICE_WINDOW_MS` 悬空导出 | ✅ 0 命中 |

---

## 五、作者回复区

> Tom，请直接在下面填写（或更简单：commit 里写 `fix(qa): P0-22 xxx` / `fix(qa): P1-16 xxx`，我会自动扫描编号）。
> 「认为不是问题」和「无法静态确认」的两类请务必回一句，否则下轮会重复报。

### ① 已处理（附 commit）

| 编号 | commit | 说明 |
|------|--------|------|
|  |  |  |

### ② 处理中

| 编号 | 计划 | 预计版本 |
|------|------|----------|
|  |  |  |

### ③ 待处理 / 不打算做

| 编号 | 决定 | 理由 |
|------|------|------|
|  |  |  |

### ④ 对本轮结论的异议 / 补充

**本轮只需要你回答一个问题**：后台「爱发电订单」tab 里的 `plan_map` 是不是空的？是否为每个在售套餐都配了 `plan_id`？
（`AFDIAN_USER_ID` / `AFDIAN_TOKEN` 我已线上确认配好了，不用再答。）

---

_本文件由 QA 新建，只增不改。QA 不修改源码。_
