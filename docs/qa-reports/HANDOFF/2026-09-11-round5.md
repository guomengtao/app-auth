# QA 交接本 · 2026-09-11 Round 5

> - 作者（Tom）：**只在本文件的「作者回复区」填写**，不要改上面的表格。
> - 双方写的是不同的文件 / 区块 → 天然零冲突，历史完整留档。

- 本轮 HEAD：`54c3f77`（上轮基线 `122ce85`，+8 提交）
- 完整报告：[`../app-auth-QA-20260911-1433.md`](../app-auth-QA-20260911-1433.md)

---

## 一、上轮（Round 4）条目复核结果

> 全部**打开代码复核**，不采信"已修"字面结论。

| # | 上轮编号 | 标题 | 状态 | 复核证据（代码定位） |
|---|---------|------|------|------|
| 1 | **P0-14** | 后台「备份/同步 → 立即执行」必 401 | ✅ **已修复** | `api/admin/health.js:36`、`:46` 的 `vercelPath` 已去掉 `&cron=1`；`:2237-2240` run-task 内部 fetch 已加 `Authorization: "Bearer " + (process.env.CRON_SECRET \|\| "")`。**但依赖 P0-15** |
| 2 | **P0-15** | CRON_SECRET 是否已配置 | ⚠️ **仍待确认** | 代码无法判定（`health.js:460` 要求 `cronSecret !== ""`）；全仓除 QA 报告外无任何配置文档/脚本提及 `CRON_SECRET` |
| 3 | **P1-9** | 解绑接口就绪但后台无入口 | ✅ **已修复** | `admin_Dx23.html:5015` 兑换码「已使用」行新增「解绑」→ `unbindRedeemCode()` `:5698`；`:5480` 激活记录行新增「Unbind」→ `unbindActivation()` `:5859`。后端 `api/admin/redeem-codes.js:311-336` 字段对得上 |
| 4 | P1-10 | `activate.html` 残留 Promise / URLSearchParams | ⚠️ **改了但改出新问题** | Promise/URLSearchParams 确已移除（新增 `parseQueryParams` `:194`、手写 thenable `:210`），**但手写 thenable 只挂了 `.then` 没有 `.catch` → 引入 P0-16** |

**说明**：P0-16 是"改 A 坏 B"的典型回归 —— 不是你改错了方向，是手写 Promise 替代品时漏了方法。**建议这次直接换回真 `Promise`**，兼容性其实比手搓的更安全（见报告建议 B）。

---

## 二、本轮待办（只放 P0 / P0-S / P1）

| # | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议 |
|---|------|------|--------|------|------|------|
| 1 | **P0-16** | 激活页点「立即激活」永久卡在「激活中...」，激活码永不显示（**线上已复现，全端型**） | P0 | ⬜ 待处理 | `activate.html:213`（只挂 `.then`）、`:236`、`:357-404`（调用链有 `.catch`）、`:410` | 最快一行：`result.catch = function(){ return result; };` 加在 `:213` 旁边。更彻底：换回 `new Promise(...)`。另外把 `setLoading(false)` 挪进 `xhr.onload`/`onerror`，杜绝以后再卡死 |
| 2 | **P0-15** | **待你确认**：CRON_SECRET 若未配置，每日 04:00 备份 / 05:00 同步永久静默 401 | P0（待确认） | ⬜ 待确认 | `api/admin/health.js:459-460`；`vercel.json` | Vercel → Settings → Environment Variables 设 `CRON_SECRET`（`openssl rand -hex 32`）；设完对 health-backup / health-sync 各点一次「立即执行」确认 success |
| 3 | **P1-11** | 首页/用户手册/教程 三页 CSS 逗号被吞，渐变背景与卡片底色全失效 | P1 | ⬜ 待处理 | `index.html:11/19/23/54/60/61`；`user-guide.html:11/20/23`；`course-guide.html:11/20/23` | 补回逗号：`linear-gradient(135deg, #0f0c29, #302b63, #24243e)`、`rgba(255,255,255,0.97)`、`rgba(0,0,0,0.5)`、`rgba(108,99,255,0.15)` |
| 4 | **P1-12** | 用户手册只给了一个下载链接（10 Pro 方屏版），其他机型会装错包 | P1 | ⬜ 待处理 | `user-guide.html:201` | 按机型列 5 个：7778(10Pro方) / 7828(9Pro方) / 7829(9胶囊) / 7830(S3·S4圆) / 7877(10胶囊) |
| 5 | **P1-13** | 手册说"换设备直接用同一个兑换码重新生成"，代码实际拒绝；且错误文案里的"联系作者"全站无联系方式 | P1 | ⬜ 待处理 | 文档 `user-guide.html:287`；代码 `api/activate.js:347-360`；文案 `:349`/`:358` | 改手册 + 把真实 QQ 群/微信写进手册和激活页错误提示 |
| 6 | **P1-14** | `login_aXs12.html` / `redeem-counts.html` 仍残留 Promise / URLSearchParams | P1 | ⬜ 待处理 | `login_aXs12.html:99/113/114/125`；`redeem-counts.html:87/101/102` | 复用 `activate.html:194-237` 的 `parseQueryParams` + XHR 封装（**记得带 `catch`**） |
| 7 | **P1-15** | 「复制激活码」用 `navigator.clipboard`，老内核下点了没反应 | P1 | ⬜ 待处理 | `activate.html:332-337` | 加 `document.execCommand('copy')` 降级 |

**QA 建议顺序**：**P0-16（一行，立刻止血）→ P0-15（1 分钟确认）→ P1-11（顺手）→ P1-13（改文案）→ P1-12 / P1-14 / P1-15（本周内）**

> P0-16 请优先于一切。它线上正在发生：用户点了激活、你这边收到通知，但用户永远看不到激活码。

---

## 三、本轮确认「不是缺陷」（避免你重复排查）

- 激活业务逻辑本体（幂等复用、换设备拒绝、错误文案、trim + 转大写归一）**全部正确**，坏的只是前端调用链。
- 爱发电 Webhook 验签失败**不会误发码**，且有 `afdian:processed` 幂等键防重复发码。
- `switch-db` 三道校验完整，禁止切到禁用库（P0-10 护栏未被本轮改动破坏）。
- `direct-activate` 四道校验完整（deviceId / ≥1 产品 / ≤20 / months 1-99）。
- 后台 11 个 tab ↔ 11 个 panel 一一对应，无空白面板。
- 全仓 `api/ lib/ scripts/ tools/` 下所有 `.js` / `.mjs` 通过 `node --check`（0 失败）。
- `scripts/clear-db.js`、`scripts/force-sync.js` 护栏仍在。
- 数据源：激活状态权威源单一 Redis，写入与校验读取同源，**不会出现"激活成功但显示未激活"**。

---

## 四、作者回复区

> 在这里逐条写结论。QA 下一轮会自动读取本文件，并**打开代码复核**是否真修（不会只看你写"已修"）。

### P0-16 激活页卡在「激活中...」
- 结论：
- 说明：

### P0-15 CRON_SECRET 是否已配置
- 结论：（请明确写「已设置」或「未设置」）
- 说明：

### P1-11 三页 CSS 逗号被吞
- 结论：
- 说明：

### P1-12 手册下载链接
- 结论：
- 说明：

### P1-13 换设备手册与代码冲突 / 缺联系方式
- 结论：
- 说明：

### P1-14 login / redeem-counts 残留 ES6 API
- 结论：
- 说明：

### P1-15 复制按钮降级
- 结论：
- 说明：

### 其他想对 QA 说的
-
