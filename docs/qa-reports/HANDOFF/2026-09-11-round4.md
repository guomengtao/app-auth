# QA 交接本 · 2026-09-11 Round 4

> - 作者（Tom）：**只在本文件的「作者回复区」填写**，不要改上面的表格。
> - 双方写的是不同的文件 / 区块 → 天然零冲突，历史完整留档。

- 本轮 HEAD：`cb730ba`（上轮基线 `ded59c6`，+3 提交）
- 完整报告：[`../app-auth-QA-20260911-0812.md`](../app-auth-QA-20260911-0812.md)

---

## 一、上轮（Round 3）条目复核结果

> 全部**打开代码复核**，不采信"已修"字面结论。

| # | 上轮编号 | 标题 | 状态 | 复核证据（代码定位） |
|---|---------|------|------|------|
| 1 | **P0-12** | `activate.html` 手环端 fetch 兼容 | ✅ **已修复** | `activate.html:194` 新增 `xhrFetch`；`:337`/`:398` 已改。全仓 HTML 无 `fetch(` 残留（仅 admin 保留） |
| 2 | **P0-13** | 兑换码换设备锁死 | ⚠️ **修一半 → 降 P1-9** | 后端 ✅ `redeem-codes.js:311-336` unbind 正确；文案 ✅ `activate.js:349`；**但 `admin_Dx23.html` 0 处「解绑」入口** |
| 3 | **P0-S-1** | `?cron=1` 鉴权绕过 | ✅ **已修复** | `health.js:459-460` 改 Bearer 校验且要求 `cronSecret !== ""`；`vercel.json` 已去掉 `&cron=1` |
| 4 | P1-4 | `index.html:140` 语法错误 | ✅ **已修复** | 已改 XMLHttpRequest；全仓 .js/.mjs `node --check` 全过 |
| 5 | P1-5 | `login_aXs12.html` fetch | ✅ **已修复** | `:97` xhrFetch、`:147` 调用 |
| 6 | P1-6 | `redeem-counts.html` fetch | ✅ **已修复** | `:85` xhrFetch、`:131` 调用 |
| 7 | P1-7 | `ev-schedule.html` const/IO | ✅ **已修复** | const/let 命中 = 0 |
| 8 | P2-3 | 失败记录 deviceId undefined | ✅ **已修复** | `activate.js:184` 提前赋值 |
| 9 | P1-8 | 后台 ES6 改造 | ⬜ **你选择延后** | QA 认可，后台桌面定位，不再重复提出 |

**感谢**：8 条一次改完，手环端三个页面 + 解绑 + cron 鉴权都到位了。

---

## 二、本轮待办（只放 P0 / P0-S / P1）

| # | 编号 | 标题 | 优先级 | 状态 | 定位 | 建议 |
|---|------|------|--------|------|------|------|
| 1 | **P0-14** | 后台「备份 / 同步 → 立即执行」必然 401 失败（cron 鉴权改了但 `vercelPath` 里两处 `&cron=1` 没同步改） | P0 | ⬜ 待处理 | `api/admin/health.js:36`、`:46`、`:2211-2213` | 去掉 vercelPath 的 `&cron=1`，并在 run-task 内部 fetch 加 `headers: { Authorization: "Bearer " + process.env.CRON_SECRET }`。或更简单：run-task 不走 HTTP，直接调用本进程函数 |
| 2 | **P0-15** | **待你确认**：CRON_SECRET 若未在 Vercel 配置，每日 04:00 备份 / 05:00 同步永久 401 静默失败 | P0（待确认） | ⬜ 待确认 | `api/admin/health.js:459-460`；`vercel.json` | ① Vercel → Settings → Environment Variables 设 `CRON_SECRET`（`openssl rand -hex 32`）；② 设完看后台「定时任务」最近执行是否 success。**你自己回复区也点了这点，请确认一下是否已设置** |
| 3 | **P1-9** | 解绑接口已就绪但后台无入口，只能 curl | P1 | ⬜ 待处理 | `admin_Dx23.html`（0 命中「解绑」）；接口 `api/admin/redeem-codes.js:311` | 兑换码列表「已使用」行加「解绑」按钮 + 二次确认，约 5 行代码 |
| 4 | P1-10 | `activate.html` 残留 `URLSearchParams` / `Promise`，手环端需实测 | P1 | ⬜ 待实测 | `activate.html:220`、`:225`、`:195` | 真机跑一次；若正常可关闭；若白屏则加正则解析降级 |

**QA 建议顺序**：**P0-15（先确认环境变量，1 分钟）→ P0-14（改 2 行）→ P1-9（加个按钮）→ P1-10（真机实测）**

> P0-14 和 P0-15 是同一根因的两面：一个断手动通道，一个可能断定自动通道。**建议一次处理完，把备份/同步双通道都恢复。**

---

## 三、本轮确认「不是缺陷」（避免你重复排查）

- 激活主链路（兑换码 → 激活码 → 幂等复用 → 换设备拒绝）**全部通**，文案清晰。
- 爱发电 Webhook → 发码 → 私信链路完整且有 `afdian:processed` 幂等，不会重复发码。
- `switch-db` 校验完整：目标库必须存在且已启用，禁止切到禁用库（P0-10 护栏仍在）。
- `direct-activate` 校验链完整（deviceId / 产品非空 / ≤20 / months 1-99 / 产品存在）。
- 后台 11 个 tab 与 panel 一一对应，无空白面板；**除定时任务两个 ▶️ 按钮外，其余栏目点了都有反应**。
- 全仓 `api/ lib/ scripts/ tools/` 下所有 .js 与 .mjs `node --check` **全部通过**。
- 数据源：用户激活状态权威源为 Redis，写入与校验读取同源，**不会出现"激活成功但显示未激活"**。

---

## 四、作者回复区

> 在这里逐条写结论。QA 下一轮会自动读取本文件，并**打开代码复核**是否真修（不会只看你写"已修"）。

### P0-14 后台备份/同步按钮 401
- 结论：
- 说明：

### P0-15 CRON_SECRET 是否已配置
- 结论：（请明确写「已设置」或「未设置」）
- 说明：

### P1-9 后台解绑入口
- 结论：
- 说明：

### P1-10 手环端 URLSearchParams 实测
- 结论：
- 说明：

### 其他想对 QA 说的
- 
