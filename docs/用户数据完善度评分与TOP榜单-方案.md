# 用户数据完善度评分 与 TOP 榜单 —— 设计方案

> 日期：2026-09-24
> 位置：后台「快捷激活 → 📊 画像汇总」
> 依赖：`tracking_events`（已在 v1.7.54 上线，历史数据已回填 692 条）

---

## 0. 先回答：以后新数据会自动写入吗？

**会自动写入。** 六个业务节点都已接上 `lib/tracking.js`，且全部走 `background.run()`（不阻塞用户请求，失败只打日志）：

| 节点 | 触发时机 | 写入位置 | 状态 |
|---|---|---|---|
| `visit` 页面访问 | 页面埋点 `POST /api/activate?section=visitor-track` | `api/activate.js` | ✅ 自动（含 index / activate / my-ip / admin + 5 个文档页） |
| `go_click` 短链落地 | 访问 `/go/:slug` | `api/go.js` | ✅ 自动 |
| `purchase_click` 购买点击 | 同上（同一次点击的另一条记录） | `api/go.js` | ✅ 自动 |
| `order` 支付订单 | 爱发电 webhook / cron 同步 | `lib/afdian-processor.js` | ✅ 自动 |
| `redeem` 发放兑换码 | 同上（跟订单同一时刻） | `lib/afdian-processor.js` | ✅ 自动 |
| `activation` / `failure` 激活结果 | 用户激活 | `api/activate.js` | ✅ 自动 |

**三点需要知道的边界**（不是 bug，是设计）：

1. **表不存在时会自愈**：`ensureTable()` 幂等，第一次写入自动建表建索引。
2. **回填不用再点**：回填只是补历史，新数据实时进。重复点也不会产生重复行（`dedupe_key` 唯一）。
3. **订单的「设备归属」是延迟补齐的**：订单来自爱发电服务端回调，**天生没有 deviceId/IP**；要在该订单的兑换码被激活后，才能用 `redeem_code` 把设备反查回订单（`enrichIdentity()`，在打开漏斗/画像时自动执行，5 分钟节流）。所以**刚支付、还没激活的订单暂时是"无设备"状态**，属正常。

---

## 1. 这个分数是干什么用的（先明确目的）

用户说的"从业务流程和数据完善度工作开展上就比较合格"，点出了真正用途：

> **这不是"用户价值分"，而是"这个用户的数据是否被完整、可信地记录下来"的体检分。**

它有三个用途：

| 用途 | 说明 |
|---|---|
| **① 采集健康度** | 分数普遍低 → 说明某个环节的埋点/字段还没打通（例如访问侧没带 deviceId），是**开发任务清单** |
| **② 标杆样本** | 满分用户 = 全链路可追溯的理想形态，拿它对照就能看出缺哪一环 |
| **③ 排查入口** | 点开高分用户的时间线，就是一条"标准链路"；点开低分用户，就能看到断点在哪 |

⚠️ 因此 UI 上必须写清"这是**数据完整度**，不是消费能力/忠诚度"，否则运营会误读。

---

## 2. 现状体检（真实数据，2026-09-24）

回填后 112 台设备、692 条事件的分布：

### 2.1 链路完整度分布

| 链路覆盖 | 设备数 | 说明 |
|---|---|---|
| 0/5（只有激活失败记录） | **34** | 激活失败但从未成功——运营重点对象 |
| 2/5 | 31 | |
| 3/5 | **45** | 最常见：订单 + 发码 + 激活 |
| 4/5 | 2 | |
| **5/5** | **0** | ← 没有人满分，因为下一条 |

### 2.2 字段覆盖率（决定性事实）

| 事件类型 | 条数 | 有 device | 有 IP | 有兑换码 | 有订单号 | 有渠道 | 有机型 |
|---|---|---|---|---|---|---|---|
| redeem | 196 | 52% | **0%** | 100% | 40% | **0%** | **0%** |
| visit | 149 | **1%** | 100% | 0% | 0% | 1% | 0% |
| failure | 132 | 100% | 100% | 98% | 0% | 9% | 17% |
| activation | 122 | 84% | 60% | 85% | 0% | 11% | **25%** |
| order | 79 | 62% | **0%** | 100% | 100% | **0%** | 0% |
| go_click | 17 | 12% | 100% | 0% | 0% | 6% | 0% |

**读出来的三个系统性缺口**：

1. **访问侧没有身份**：`visit` 只有 1% 带 deviceId —— 因为 `visitor_logs.params` 是今天才加的列，历史数据为空。→ 直接用事实回答"为什么访问→支付算不出来"。
2. **订单/兑换码没有 IP**：0%，服务端回调拿不到买家 IP（设计限制，不是缺陷）。
3. **渠道与机型覆盖极低**（0~25%）：`c` / `m` 参数只有 App 生成的链接才带，手动激活（输入 deviceId）没有。

**结论：评分模型不能只判断"有没有事件"。** 如果用"5 步各 20 分"，现在 90% 的人都只能拿 60 分以下，榜单失去区分度，而且**扣分点全在用户无法控制的地方**。所以必须拆成"身份可识别度 + 链路完整度 + 属性丰富度 + 归属地完整度"四个维度，并把"平台能力缺口"与"用户行为差异"区分开。

---

## 3. 评分模型（满分 100）

### 3.1 四个维度与权重

| 维度 | 权重 | 回答什么问题 |
|---|---|---|
| **A. 身份可识别度** | 25 | 这个人能被稳定、唯一地认出来吗？ |
| **B. 链路完整度** | 40 | 从访问到激活，关键节点是否都能找到记录？ |
| **C. 属性丰富度** | 20 | 设备/渠道/订单信息是否够拿来分析？ |
| **D. 归属地完整度** | 15 | 省 / 市 / 区县是否都有？ |

### 3.2 A. 身份可识别度（25 分）

| 项 | 分值 | 判据 |
|---|---|---|
| 有设备标识 | 10 | `device_full` 非空 |
| 设备标识完整（非 4 位截断） | +6 | `length(device_full) > 4`（4 位碰撞概率详见 `用户全链路追踪与画像汇总-方案.md` §4.3） |
| 有 IP | +5 | 任一事件 `ip` 非空 |
| 设备标识一致（无多设备冲突） | +4 | 该用户下 `count(distinct device_full) == 1` |

**不参与加分的风险项**（只做标记，不扣分）：模拟器指纹、同一 IP 多设备、购买/激活 IP 不同。

> 为什么不扣分：这些是**平台侧识别能力**问题，不是数据缺陷。扣分会打击真实性反而误导。

### 3.3 B. 链路完整度（40 分，5 步 × 8 分）

| 步骤 | 8 分条件 |
|---|---|
| ① 访问（visit / go_click） | 有任一条访问类事件 |
| ② 购买意图（go_click / purchase_click） | 有短链点击或购买点击 |
| ③ 支付订单（order） | 有订单事件 |
| ④ 发放兑换码（redeem） | 有兑换码事件 |
| ⑤ 激活（activation） | 有激活成功事件 |

**顺序校验（最高扣 8 分）**：必须满足 `访问 ≤ 点击 ≤ 支付 ≤ 发码 ≤ 激活`。违反一项扣 4 分，最多扣 8 分——用于发现"时间线不合理"（多账号共用 IP 的典型症状）。

**说明**：① ② 现在几乎拿不到分（历史数据缺 params）。这是**有意为之**——榜单要如实反映"访问侧采集还没打通"，而不是粉饰。等新数据积累，这两项会自然涨上来。

### 3.4 C. 属性丰富度（20 分）

| 项 | 分值 | 判据 |
|---|---|---|
| 机型 / 产品名 | 6 | `payload->>'model'` 长度 ≥ 3（`ap` 这类垃圾值不算） |
| App / ROM 版本 | 4 | `payload->>'rom'` 非空（如 `1.6.69`） |
| 渠道 | 5 | `channel` 非空（如 `t-9p-d`） |
| 订单侧信息（昵称 / 金额 / 套餐） | 5 | 订单事件 payload 里有 `user_name` 或 `amount` |

### 3.5 D. 归属地完整度（15 分）

| 项 | 分值 | 判据 |
|---|---|---|
| 省（`region_zh` / `region`） | 5 | `ip_lookups` / Vercel 头部命中 |
| 市 | 5 | 同上 |
| 区县 | 5 | 腾讯位置服务命中 |

### 3.6 档次标签

| 分数 | 标签 | 含义 |
|---|---|---|
| 85–100 | 🟢 全链路可追溯 | 标杆样本，任何分析都能做 |
| 65–84 | 🔵 基本完整 | 主流程齐全，个别属性缺 |
| 40–64 | 🟡 部分可追溯 | 能认出人、能对齐交易，但缺访问侧/属性 |
| 0–39 | 🔴 数据稀疏 | 只有失败记录或单一事件，需人工介入 |

### 3.7 算一个真实例子

样本：一台有「订单 + 兑换码 + 激活」且带机型的设备（当前 45 台的主流形态）

```
A 身份：有设备 10 + 设备完整 6 + 有 IP 5 + 单一设备 4                = 25 / 25
B 链路：访问 0 + 点击 0 + 订单 8 + 发码 8 + 激活 8                  = 24 / 40
         （① ② 拿不到分：历史访问没有 params）
C 属性：机型 6 + 版本 4 + 渠道 0（手动激活没带 c）+ 订单 5           = 15 / 20
D 归属地：省 5 + 市 4（无区县）                                     = 9 / 15
────────────────────────────────────────────────────────────────
合计 = 73 分 → 🔵 基本完整
```

缺口清单会直接告诉运营/开发：**「缺访问侧 deviceId」「缺渠道参数」「缺区县」** —— 这就是"数据完善度工作"的抓手。

---

## 4. TOP5 榜单规则

### 4.1 排序与准入

- **准入条件**（防止"只有一条记录但字段全"的噪声设备霸榜）：必须有 `activation` 或 `order` 事件，即**产生了真实业务价值**。
- 按总分降序，**并列时依次比较**：链路完整度 → 身份可识别度 → 事件数 → 最近活跃时间。
- 榜单项展示：分数、档次标签、设备掩码、机型、渠道、地区、5 步打钩图、缺口清单。

### 4.2 榜单的两种形态

| 形态 | 内容 | 价值 |
|---|---|---|
| **🏆 数据完善度 TOP5** | 得分最高的 5 台 | 标杆样本，用来对照"理想链路长什么样" |
| **📉 缺口聚合榜** | 各项检查点的**未命中设备数**排序 | 直接产出开发优先级：例如「访问侧缺 deviceId：98% 未命中」→ 一目了然这是最大的坑 |

**缺口聚合榜比 TOP5 更有行动价值**，建议两个一起上。

### 4.3 ⭐ 时间窗口：这是缺口榜的核心设计

用户提的关键点：**历史数据的缺口几乎不可能补（老记录里本来就没有 deviceId / 渠道参数），只有新阶段的用户才能体现改进。** 所以窗口设计必须区分"整体健康度"和"改进效果"。

#### 4.3.1 窗口选择

| 选项 | 用途 |
|---|---|
| 近 1 天 | 看最新采集是否正常（发布后的快速验证） |
| **近 7 天（默认）** | 当前阶段的采集健康度 |
| 近 30 天 | 趋势观察 |
| 近 90 天 | 含历史包袱，用于对比 |
| 自定义 | 指定起止日期（如"某次埋点上线之后"） |

#### 4.3.2 两种统计口径（`scope`）—— 必须分开

| scope | 定义 | 回答什么 |
|---|---|---|
| `active` | 窗口内**有任何事件**的设备 | 当前阶段整体采集健康度（含老设备的新行为） |
| **`new`（默认）** | **窗口内才首次出现的设备**（该设备全表最早事件 ≥ 窗口起点） | **只看新用户** → 这才是"优化有没有生效"的真实答案 |

举例说明差别（当前真实数据）：老设备在窗口内又激活一次，它**仍然没有** `visit` 的 deviceId（老代码写的数据），会一直拉低 `active` 口径的分数；而 `new` 口径里全是新代码产生的数据，分数才代表现在的真实水平。

**两个口径并列展示**，中间用箭头显示差异：

```
📉 数据缺口排行（近 7 天 · 新设备口径 · 样本 24 台）
   访问侧缺 deviceId      2 / 24  (8%)   ↓ 较上周 -82%
   缺渠道参数            18 / 24  (75%)  —   ← 还没改，是下一个目标
   缺区县                21 / 24  (88%)  ↑ 较上周 +6%
   缺机型                 6 / 24  (25%)  ↓ 较上周 -40%
   ────────── 整体口径（含历史设备 112 台）──────────
   访问侧缺 deviceId    110 / 112 (98%)
```

#### 4.3.3 窗口对比（周环比）

每个检查项都返回 **本窗口 / 上一等长窗口** 两组数字，UI 显示 `↓ -82%` / `—` / `↑ +6%`：

- `↓` 表示缺口在缩小 → **改进生效**
- `—` 表示没变化 → 尚未动过的坑
- `↑` 表示缺口在扩大 → 可能是新功能引入了新缺口，要立刻查

**这是这张榜最有价值的产出：一眼看出"哪些坑在收敛、哪些还没碰、哪些在恶化"。**

#### 4.3.4 基准日 `SCORE_EPOCH`

埋点补齐（`visitor_logs.params` 列、文档页 `visitor-track`）发生在 **2026-09-24**。

- 落地一个常量 `SCORE_EPOCH = "2026-09-24"`；
- 窗口起点早于该日时，UI 提示：**"该窗口包含埋点补齐前的数据，分数天然偏低，不代表当前采集水平"**；
- 分数排行建议默认从 `SCORE_EPOCH` 起算，避免历史数据污染榜单。

#### 4.3.5 样本量保护

窗口 + `new` 口径下样本可能很小（早期只有几台）。规则：

- 样本 < 10 台时，排名仅作参考，UI 标注 **"⚠ 样本仅 N 台，波动大"**；
- 样本 = 0 时显示"该窗口内无新设备"，不做百分比计算（避免出现 0/0）。

### 4.4 口径与防坑

1. **只统计有 device 的设备**，纯 IP 访客不进入榜单（无法判定身份）。
2. **模拟器/共享设备照常参评但打标记**，不排除——否则会掩盖"这类设备数据最全"的事实（模拟器往往参数最全）。
3. **分数随时间波动是正常的**：新数据补进来时低分设备会涨分。UI 要标注"按当前窗口已有记录计算"。
4. **不要把分数导出成"用户价值"指标**（见 §1 的警示）。
5. **不要跨窗口比较单个设备的分数**：同一设备在 7 天窗口和 90 天窗口的分数必然不同（事件集合不同）。榜单必须显式标注窗口。

---

## 5. 数据来源与计算

### 5.1 全部来自 `tracking_events`（无需新表）

```sql
-- ① 按设备聚合出打分所需的事实
select device_full,
       bool_or(kind='visit' or kind='go_click')            as has_visit,
       bool_or(kind='go_click' or kind='purchase_click')   as has_click,
       bool_or(kind='order')                               as has_order,
       bool_or(kind='redeem')                              as has_redeem,
       bool_or(kind='activation')                         as has_act,
       bool_or(kind='failure')                             as has_fail,
       max(channel)                                        as channel,
       max(ip)                                             as ip,
       min(ts) as first_ts, max(ts) as last_ts,
       count(*)::int as events,
       max(payload->>'model') as model,
       max(payload->>'rom')   as rom
from tracking_events
where ts >= now() - interval '30 days' and device_full <> ''
group by device_full;

-- ② 归属地用 ip_lookups 富化（复用 lib/geo-zh.resolveDisplayGeo）
select ip, region_zh, city_zh, district, country, region, city from ip_lookups where ip = any($1);

-- ③ 顺序校验：取每类事件的最早时间戳
select device_full, kind, min(ts) as t from tracking_events
where device_full <> '' and kind in ('visit','go_click','purchase_click','order','redeem','activation')
group by device_full, kind;
```

### 5.2 建议模块 `lib/user-score.js`

```
scoreDevices(days, opts) → {
  leaderboard: [ { device(masked), score, grade, dims:{identity,journey,attribute,geo},
                   steps:[bool×5], missing:[...], model, channel, region, city, events, lastTs } ],
  gaps:       [ { key, label, missCount, total, pct } ],   // 缺口聚合榜
  summary:    { devices, avgScore, byGrade:{A,B,C,D} }
}
```

- 一次 SQL 拉事实 + 一次 `ip_lookups` 富化，**在内存里算分**（设备量千级，毫秒级）。
- 读时计算，**不物化**（口径随规则调整立即生效）；若将来设备上万，再考虑每日快照表。

### 5.3 接口

| 接口 | 说明 |
|---|---|
| `GET /api/admin/health?section=stats&sub=user-score&days=7&scope=active&limit=5` | TOP5 + 分布概览 |
| `GET /api/admin/health?section=stats&sub=data-gaps&days=7&scope=new&compare=1` | 缺口聚合榜（含周环比） |

参数：

| 参数 | 取值 | 默认 | 说明 |
|---|---|---|---|
| `days` | 1 / 7 / 30 / 90 | **7** | 统计窗口 |
| `from` / `to` | `YYYY-MM-DD` | — | 自定义窗口（与 `days` 二选一） |
| `scope` | `active` / `new` / `both` | `new`（缺口榜）/ `active`（TOP5） | 见 §4.3.2 |
| `compare` | 0 / 1 | 1 | 是否返回上一等长窗口用于环比 |
| `limit` | 1~20 | 5 | TOP 数量 |

返回值（缺口榜）关键结构：

```json
{
  "success": true,
  "window": { "from": "2026-09-18", "to": "2026-09-24", "days": 7, "epochWarning": false },
  "scope": "new",
  "sample": { "new": 24, "active": 131, "lowSample": false },
  "gaps": [
    { "key": "visit_device", "label": "访问侧缺 deviceId",
      "miss": 2, "total": 24, "pct": 0.08,
      "prevPct": 0.90, "delta": -0.82, "trend": "down" }
  ],
  "gapsAllScope": [ { "key": "visit_device", "miss": 110, "total": 112, "pct": 0.98 } ]
}
```

⚠️ 注意：`section=stats` 有 GET 守卫，**这两个是只读接口，放在守卫之后即可**（写操作才需要提前，参考 `tracking-backfill` 的 405 教训）。

⚠️ **不要为这两个接口新建文件**：`api/` 已有 12 个函数（详见 `docs/architecture.md` 的「Vercel 函数文件数」规则），必须挂在 `api/admin/health.js` 的 `sub=` 分支下。

### 5.4 UI（画像汇总页）

窗口选择器放在卡片顶部，**两个卡片共用**（切换后同时刷新）：

```
[ 近 1 天 ] [ 近 7 天✓ ] [ 近 30 天 ] [ 近 90 天 ] [ 自定义 ▾ ]     口径: (新设备✓) (全部) (并列)

┌─ 🏆 数据完善度 TOP5 ─────────────────────── 窗口 09-18 ~ 09-24 ────┐
│ #1  92 🟢  d4cd…c786  Vela Watch  t-9p-d  深圳·南山  5/5 步  [详情] │
│ #2  88 🟢  8550…818a  Mi Band 9   t-10-r  杭州·西湖  5/5 步  [详情] │
│ ...                                                                │
│ 平均分 61 ｜ 🟢3 🔵1 🟡1 ｜ 样本 24 台                              │
└────────────────────────────────────────────────────────────────────┘
┌─ 📉 数据缺口排行 ────────── 近 7 天 · 新设备 24 台 · 较上周 ────────┐
│ 访问侧缺 deviceId    2/24  (8%)   ↓ -82%   ✅ 改进已生效            │
│ 缺渠道参数          18/24 (75%)   —        尚未处理                │
│ 缺区县              21/24 (88%)   ↑ +6%    ⚠ 在恶化，需排查        │
│ ────────── 整体口径（含历史设备 112 台）────────────────────────── │
│ 访问侧缺 deviceId  110/112 (98%)                                   │
└────────────────────────────────────────────────────────────────────┘
```

- 窗口起点早于 `SCORE_EPOCH`（2026-09-24）时，卡片顶部显示黄条：**"该窗口含埋点补齐前的数据，分数天然偏低，不代表当前采集水平"**。
- 每行「详情」→ 复用现有 `openUserJourney(deviceId)` 跳到「用户画像」子 tab。
- 每行展开可看**缺口清单**（还差哪几项才能满分）。
- 样本 < 10 台时，百分比旁边显示 **"⚠ 样本少"**，不做趋势箭头。

---

## 6. 分期实施

| 阶段 | 内容 | 说明 |
|---|---|---|
| **一期** | `lib/user-score.js` + `sub=user-score` + `sub=data-gaps`（含窗口 / scope / 环比）+ 画像汇总页两个卡片 + 窗口选择器 | 纯读，不动写入侧，风险最低 |
| 二期 | 分数快照（每日存一份，用于看"采集健康度"随时间的变化曲线，替代实时环比） | 新表 `user_score_daily(day, device_full, score, dims jsonb)` |
| 三期 | 与人工校正联动：`identity_overrides` 命中后重算分数 | 让修正立即反映到榜单 |

**建议先做一期**：它立刻能回答两个问题——"新阶段的采集到底打通了几成"（缺口榜新设备口径 + 环比）和"理想形态长什么样"（TOP5）。

### 6.1 落地时的硬性约束

1. **不得新增 `api/` 下的文件**：两个接口都挂在 `api/admin/health.js` 的 `section=stats&sub=` 下（见 §5.3）。
2. **权重集中成常量对象**（如 `SCORE_WEIGHTS`），便于按分布调参。
3. **窗口/口径参数必须原样回显在接口响应里**（`window` / `scope` / `sample`），避免前端算错或后端默认值变更后 UI 不刷新。

---

## 6.2 一期实现状态（已落地）

| 项 | 状态 |
|---|---|
| `lib/user-score.js` | ✅ `analyze({days,from,to,scope,limit,compare})`，权重集中在 `WEIGHTS` 常量，`SCORE_EPOCH = "2026-09-24"` |
| 接口 | ✅ `section=stats&sub=user-score`（完整报告）/ `sub=data-gaps`（只返回缺口部分）；**未新增任何 api 文件**，挂在既有 `health.js` 下 |
| 面板 | ✅ 「📊 画像汇总」新增窗口选择器（近 1/7/30/90 天）+ 口径选择（只看新设备 / 全部设备），以及三张新卡片：🏆 TOP5、📉 缺口排行（含环比 + 整体口径对照）、🎯 采集覆盖率（事件口径） |
| 趋势 | ✅ `↓ 改进生效 / — 持平 / ↑ 恶化`，样本 < 10 台时改为显示「样本少」不出箭头 |
| 基准日提示 | ✅ 窗口起点早于 `SCORE_EPOCH` 时卡片顶部出黄条 |

### 实现中修掉的两个坑

1. **缺失项 key 静默失配**：打分往 `missing` 里推的是 `has_click`，而缺口榜定义用的是 `no_click` → **缺口榜全是 0 且不报错**。现在两边 key 逐字一致，并在 `GAP_DEFS` 上加了注释强制同步。
2. **`scoreOne` 忘返回 `device_full`** → TOP5 的设备掩码与「详情」跳转全空。已补。

### 6.3 补充修正（同日追加）

**① 机型字段取错了来源（假缺口）**

原实现取 `device_info.model`（激活 URL 的 `m` 参数），但实际数据里 `m=ap` 是**垃圾值**，真正的机型在 `p` 参数（product）：

```
/activate.html?deviceId=220cf976…&m=ap&p=REDMI%20Watch%206&o=0&v=1200&t=watch&s=rect&w=432&h=514
                     ↑ 垃圾值              ↑ 真机型
```

修复：

- 新增 `lib/tracking.js pickModel(model, product)`：长度 ≥ 3 才算有效，两者都有效取更长的（`m=ap` → 取 `p=REDMI Watch 6`）。
- `api/activate.js`（激活成功 / 复用 / 失败 / 推送 payload）、`lib/tracking.js` 回填、`lib/user-journey.js` 全部改走 `pickModel`；事件的 `payload` 额外存 `product`。
- 新增 `tracking.enrichModels()` 回填历史：扫 `auth:activation_codes` 反查 `device_info.product`，用一条 multi-row `UPDATE ... FROM (VALUES ...)` 修正 `payload.model`（本机执行修正 **92 行**）；它被放进 `background.run`（后台执行）以免阻塞读接口，并有独立 10 分钟节流。
- 结果：TOP1 从 78 分 → **84 分**，机型列从 `(未上报)` → `REDMI Watch 6`，**「缺机型 100%」这条假缺口从榜上消失**，平均分 59 → 62。

> 教训：**缺口榜的第一名先别急着当开发任务**，要先用真实样本核对"这个字段到底是没采，还是取了错的字段"。这次就是后者。

**② 用户画像页显示该用户自己的得分**

- `lib/user-score.js` 新增 `scoreDevice(device, days)`：只查这一台设备（2 条查询，不走 `analyze()` 拉全库），返回同一套口径的得分 / 维度 / 链路 / 缺口。
- 接口复用：`section=stats&sub=user-score&device=<deviceId>`。
- 「快捷激活 → 👤 用户画像」页在身份卡下方新增评分卡：总分 + 四维度进度条 + 五步链路 + 「还差 xxx」。

**③ Vercel 函数文件数 12 → 10（达标）**

见 `docs/architecture.md`：删除无人调用的 `api/admin/clear-rate-limit.js`；`api/admin/records.js` 合并进 `health.js` 的 `section=records`（前端 `loadRecords()` 改调 `/api/admin/health?section=records&...`，参数与返回不变），并把内部 `pipeline` 换成 `mget`。

### 性能（重要）

生产库单次查询 RTT 实测 ≈ 300ms（本地直连；Vercel 同区更低），**首连接可达 4~10s**。因此实现上做了三件事：

- **查询合并到 3 条**：把「当前窗口 + 上一等长窗口 + 覆盖率 + 顺序校验」全部塞进一条按 `(窗口, 设备, 事件类型)` 分组的 SQL，在内存里 `fold()` 聚合。曾是 10 条查询，实测 4~10 秒（有撞 Vercel 10s 上限的风险）。
- **进程内缓存 60s**：同一窗口 + 口径的重复请求直接复用（面板反复刷新 / 漏斗与画像同时拉），命中后 0ms。
- 实测：首次 ~3–7s（含建连，本地），缓存命中 0ms。

> ⚠️ 若生产环境实测仍偏慢，下一步是把 `days` 上限收紧或落到每日快照表（§6 二期），而不是继续加查询。

## 7. 风险与边界

1. **分数会被误读为用户价值**：UI 必须在标题处写明"数据完整度 ≠ 消费能力"，否则运营会拿它做用户分层。
2. **权重是主观的**：当前权重（25/40/20/15）基于"身份可靠性 > 链路完整 > 属性"的判断，落地后建议观察一周分布再微调；建议把权重集中在一个常量对象里，便于调整（类似 §3.1 表格直接映射代码常量）。
3. **历史数据与新数据不可比**：2026-09-24 前后的设备，因为 `params` 列刚加、文档页埋点刚补，分数天然有断层。**上线后 1~2 周内的分数才有横向可比性**，UI 应标注起始日期。
4. **不要用分数做准入门槛**（例如限制低分用户激活），它是观察工具，不是风控规则。
5. **性能**：一期按设备聚合全窗口数据，设备到万级时需要加 `days` 限制或物化；当前千级无压力。
