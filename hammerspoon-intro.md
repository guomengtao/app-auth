# Hammerspoon 能实现什么

Hammerspoon 是一款强大的 macOS 自动化工具，通过 Lua 脚本与 macOS 系统深度交互，几乎可以实现你能想到的所有自动化需求。

---

## Hammerspoon vs 命令行控制 Mac

| 维度 | 命令行 (shell/osascript) | Hammerspoon |
|------|--------------------------|-------------|
| **触发方式** | 手动执行，或靠 cron/launchd 定时轮询 | **事件驱动**：窗口变化、设备插拔、WiFi 切换、按键等系统事件自动触发 |
| **实时性** | 被动轮询，有延迟且浪费 CPU | **实时响应**，事件发生瞬间执行，零 CPU 空转 |
| **窗口交互** | 非常有限，AppleScript 也很慢 | **原生 API 直接操控窗口**，毫秒级响应 |
| **全局快捷键** | 需要借助第三方工具或 Automator | **内置支持**，任意按键组合直接绑定 Lua 函数 |
| **菜单栏交互** | 做不到 | **原生菜单栏图标 + 下拉菜单** |
| **鼠标/触控板手势** | 完全不行 | 识别鼠标手势、多点触控 |
| **开发体验** | Shell/AppleScript 语法晦涩 | Lua 语言简洁清晰，API 设计良好 |
| **常驻后台** | 需要 launchd 配合 | 自动常驻菜单栏，随时可用 |

**一句话总结**：命令行是你主动下指令去操控 Mac；Hammerspoon 是 Mac 系统发生的事情主动通知你的脚本，你写规则自动响应。一个是**被动操作**，一个是**事件驱动自动化**。

**举例**：插上外接键盘时自动切换输入法。

- 命令行：每秒 `system_profiler` 轮询 USB，延迟 1 秒，持续占 CPU
- Hammerspoon：USB 插拔事件自动触发，零延迟，零 CPU

---

## 1. 窗口管理

- **窗口移动与缩放**：将窗口移动到任意位置，调整到任意大小
- **窗口平铺**：实现类似 i3/Amethyst 的自动平铺布局（左右分屏、上下分屏、网格布局）
- **窗口网格吸附**：将窗口吸附到屏幕的 1/2、1/3、2/3、1/4 等位置
- **多显示器窗口迁移**：一键将窗口移动到指定显示器
- **窗口居中**：一键将窗口居中显示
- **窗口全屏/最大化**：控制窗口进入或退出全屏模式
- **窗口置顶**：将任意窗口固定在所有窗口之上

## 2. 应用管理

- **应用启动与退出**：通过快捷键启动或退出指定应用
- **应用切换**：快速切换到正在运行的应用
- **应用隐藏/显示**：隐藏或显示指定应用
- **应用布局记忆**：保存和恢复应用的窗口布局
- **应用焦点追踪**：监控当前焦点应用并执行相应操作

## 3. 键盘快捷键与热键

- **全局快捷键**：绑定任意按键组合执行自定义操作
- **模态快捷键**：实现类似 Vim 的多层快捷键模式
- **按键重映射**：将某个按键重新映射为另一个按键或组合键
- **超级键（Hyper Key）**：将 CapsLock 等键改造成 Hyper 键，扩展快捷键空间
- **应用特定快捷键**：仅在特定应用下生效的快捷键

## 4. 菜单栏扩展

- **自定义菜单栏图标**：在菜单栏显示自定义图标
- **下拉菜单**：点击菜单栏图标弹出自定义菜单，包含任意操作
- **状态显示**：在菜单栏实时显示 CPU 温度、内存使用、电池状态、网络速度等信息

## 5. 系统监控

- **CPU 使用率监控**：实时获取 CPU 负载
- **内存监控**：监控内存使用情况，内存不足时发出警告
- **电池状态**：获取电池电量、充电状态、循环次数、健康度
- **温度传感器**：读取 CPU、GPU 等硬件温度
- **网络流量监控**：监控上传/下载速度
- **磁盘空间监控**：监控磁盘剩余空间

## 6. 剪贴板管理

- **剪贴板历史**：记录剪贴板历史，随时调用之前复制的内容
- **剪贴板过滤**：粘贴时自动去除格式、转换大小写等
- **多剪贴板**：维护多个独立的剪贴板

## 7. 网络管理

- **WiFi 管理**：切换 WiFi 网络、获取当前 SSID、信号强度
- **网络可达性检测**：检测是否能访问某个网站或服务
- **HTTP 请求**：发起 HTTP 请求，获取数据或调用 API
- **搭建本地 HTTP 服务器**：可以直接在本地启动一个 Web 服务器
- **VPN 状态监控**：监控 VPN 连接状态
- **代理切换**：自动检测网络环境并切换代理设置

## 8. 音频管理

- **音频设备切换**：切换输入/输出音频设备（耳机、音箱、麦克风）
- **音量控制**：调节系统音量，静音/取消静音
- **音频设备监控**：检测音频设备的插拔事件，自动切换设备
- **均衡器**：设置系统均衡器参数

## 9. 显示器与屏幕

- **屏幕亮度调节**：调节显示器亮度（支持外接显示器）
- **分辨率切换**：快速切换屏幕分辨率
- **显示器排列**：调整多显示器的相对位置
- **屏幕锁定/睡眠**：锁定屏幕、进入睡眠、唤醒
- **夜间模式**：定时切换深色/浅色模式

## 10. 通知与提醒

- **系统通知推送**：自定义条件和内容发送 macOS 原生通知
- **定时提醒**：类似番茄钟、喝水提醒、久坐提醒
- **弹窗对话框**：显示自定义对话框，获取用户确认或输入

## 11. 鼠标与触控板

- **鼠标位置控制**：获取和设置鼠标光标位置
- **鼠标手势**：识别鼠标绘制的手势图案并触发操作
- **鼠标点击**：模拟鼠标点击（左键、右键、中键）
- **触控板手势**：绑定多点触控手势

## 12. 外接设备

- **USB 设备检测**：检测 USB 设备插入/拔出事件
- **键盘型号识别**：识别当前使用的键盘，自动切换键位布局
- **外接显示器检测**：外接显示器连接/断开时自动调整窗口布局

## 13. 文件系统

- **文件监控**：监控指定目录的文件变化（新增、修改、删除）
- **自动文件整理**：根据规则自动移动、重命名文件（如自动整理下载文件夹）
- **定时备份**：定时将文件备份到指定位置

## 14. 定时任务

- **定时执行**：在指定时间或间隔执行任务
- **空闲检测**：检测到用户一段时间没有操作后执行任务（如自动锁屏）
- **日程触发**：根据日历事件触发自动化

## 15. 辅助功能

- **文本展开**：输入简写自动展开为完整文本（如 `@@` 展开为邮箱地址）
- **拼写纠错**：自动替换常见拼写错误
- **中英文输入法自动切换**：根据当前应用或场景自动切换输入法

## 16. 位置感知

- **地理位置**：获取当前位置信息
- **地理围栏**：进入或离开某个区域时触发操作（如到公司自动静音）
- **时区检测**：根据位置自动调整相关设置

## 实用场景举例

| 场景 | 实现方式 |
|------|----------|
| 一键打开开发环境（终端+IDE+浏览器） | 应用管理 + 窗口布局 |
| 离开座位自动锁屏 | 空闲检测 + USB 设备检测（键盘断开） |
| 连接外接显示器时自动调整布局 | 显示器检测 + 窗口管理 |
| 开会时间自动静音、开启勿扰模式 | 定时任务 + 音频管理 |
| 选中文字一键翻译/搜索 | 快捷键 + HTTP 请求 |
| 下载文件夹自动整理 | 文件监控 + 文件系统操作 |
| 通过划动触控板触发不同操作 | 鼠标手势识别 |

---

## 相关链接

- 官方网站：[https://www.hammerspoon.org](https://www.hammerspoon.org)
- 官方文档：[https://www.hammerspoon.org/docs/](https://www.hammerspoon.org/docs/)
- 社区配置分享：[https://github.com/Hammerspoon/hammerspoon/wiki](https://github.com/Hammerspoon/hammerspoon/wiki)

> 以上所有功能均通过 Lua 脚本实现，配置灵活度极高，几乎可以满足任何 macOS 自动化需求。